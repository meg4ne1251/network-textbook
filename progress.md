# 進捗管理ファイル

このファイルは、ネットワーク技術教科書プロジェクトの進捗を記録し、
セッション(会話)をまたいで作業を引き継ぐためのものです。

**新しいセッションを始めるときは、まずこのファイル全体を読んでから、
「次のステップ」に記載された箇所から着手してください。**

---

## 現在の状態

- 完了ステップ: **Step 0〜34 すべて完了**(第1部〜第7部、全34ステップ)
- 拡張フェーズ: 第7部(QoS、`07_qos/`)完結。ロードマップ(00_overview/01_roadmap.md)への
  第7部組み込みと README の【執筆中】表記の解消も完了
- **現在のフェーズ: 検証フェーズ(裏取り)** — 本文執筆は一段落し、`shared/verification_todo.md`
  に整理した要検証項目(Tier A〜D)を一次情報源(RFC / IEEE 規格 / 公式ドキュメント)で
  裏取りする作業に移行した。進め方は Tier A を上から順に潰す → Tier B 一括更新 →
  Tier C は免責注記の一貫性確認。1項目=1確認=1コミットを基本とし、確認・修正の記録は
  下記「検証フェーズ ログ」に追記する。
- 次のステップ(検証): Tier A の続き(A2 以降)。
- 次のステップ(執筆・保留): 次の拡張分野(README の拡張予定表:第8部 セキュリティ基礎 /
  第9部 ネットワーク自動化)に着手する場合は、第6部・第7部のときと同様に、まず CLAUDE.md へ
  章構成と拡張ステップ表を追記してから始める。

---

## 検証フェーズ ログ

`shared/verification_todo.md` の各項目を裏取りした記録。1項目=1エントリで追記する。
状態の詳細(参照した版・セクション番号)は `verification_todo.md` の「状態」欄にも反映する。

### A1: `02_vlan_vxlan_evpn/01_vlan_basics.md` — CFI→DEI 再定義の版 (完了日: 2026-07-13)

- 結果: 事実は正しい。DEI(Drop Eligible Indicator)としての用法は **IEEE 802.1ad-2005**
  が S-TAG に導入したのが先で、802.1ad を統合した **IEEE 802.1Q-2011** で C-TAG の当該ビット
  (初版では CFI = Canonical Format Indicator)も DEI に再定義された。
- 対応: 本文が「現行仕様で再定義」と版を曖昧にしていたため、802.1ad-2005 / 802.1Q-2011 の
  順序と版を明示する記述に更新。
- 情報源: IEEE 802.1ad-2005 / 802.1Q-2011(全文は非公開のため、IEEE 802.1 資料・Wikipedia
  802.1ad 等の二次情報で版と経緯を確認)。

### A2: `03_bgp/01_bgp_basics.md` — BGP タイマー既定値 (完了日: 2026-07-13)

- 結果: 事実は正しい。RFC 4271 §10 の推奨(既定)値は ConnectRetryTimer 120 秒 /
  Hold Time 90 秒 / KeepaliveTimer 30 秒。§4.4「KEEPALIVE は Hold Time の 1/3 を目安、
  1 秒より高頻度に送ってはならない(MUST NOT more frequently than one per second)」、
  §4.2「受信 OPEN の Hold Time と自分の設定値の小さいほうを採用」も本文の記述と一致。
  本文併記の「実装既定 180 秒 / KEEPALIVE 60 秒」は Cisco IOS・FRR 等の慣例で、
  本文でも RFC 推奨値と区別して明記済み。
- 対応: 本文は正確。検証ルール(確認したセクション番号を残す)に沿って
  「RFC 4271 Section 10」「Section 4.4」の節番号を本文に補記。
- 情報源: RFC 4271 §4.2 / §4.4(原文で確認)、§10(WebFetch が長文で切れたため
  WebSearch で値を確認。tech-invite 版 RFC 4271・networkstraining 等)。

### A3: `03_bgp/03_path_attributes.md` — MED 未設定の既定値 (完了日: 2026-07-13)

- 結果: 本文の断定「MED 未設定は最小値 0(最優先)扱いが RFC 4271 の規定」は正しい。
  RFC 4271 §9.1.2.2 は「route n が MULTI_EXIT_DISC を持たない場合、最小値(0)を
  割り当てる」と規定。ただし RFC 4451(MED Considerations)によれば、初期の
  BGP 仕様は逆に**最悪値 2³²−1** を割り当てる規定であり、実装間で扱いが
  「地図全体に散らばっている(all over the map)」ほど不統一だった経緯がある。
  本文既存のトラブルシューティング(missing-as-worst ノブへの言及)はこの名残。
- 対応: 本文にセクション番号 §9.1.2.2 を明記し、RFC 4451 が記録する歴史的経緯
  (初期仕様は最悪値だった/実装差はその名残)を1文追記して、断定に注釈を付けた。
- 情報源: RFC 4271 §9.1.2.2(RFC 4451 経由で条文文言を確認)、RFC 4451(原文で確認)。

### A4: `03_bgp/05_mp_bgp.md` — IPv4 固有3箇所の引用 (完了日: 2026-07-13)

- 結果: 本文の引用は正確。RFC 4760 §1 原文「The only three pieces of information
  carried by BGP-4 that are IPv4 specific are (a) the NEXT_HOP attribute … (b)
  AGGREGATOR … and (c) NLRI …」と一致。AGGREGATOR は MP-BGP で拡張せず NLRI・
  NEXT_HOP だけを差し替え可能にした、という本文の整理も妥当。
- 対応: 本文引用に「Section 1」を明記。系譜が RFC 2283→4760 と中間版を飛ばして
  いたため、RFC 2858(2000)を補って正確化。引用文の (a)(c) の括弧補足も原文に合わせた。
- 情報源: RFC 4760 §1(原文で確認)。obsoletes は RFC 2858(RFC 2283 は §11 で比較言及)。

### A5: `03_bgp/06_large_scale_design.md` — RR 経路選択修正の挿入位置 (完了日: 2026-07-13)

- 結果: 本文の記述「BGP Identifier 比較の直後(ピアアドレス比較の前)に CLUSTER_LIST
  短優先を挿入」は正確。RFC 4456 §9 原文は「in Step f) the ORIGINATOR_ID SHOULD be
  treated as the BGP Identifier of the BGP speaker that has advertised the route」
  「the following rule SHOULD be inserted between Steps f) and g): a BGP Speaker
  SHOULD prefer a route with the shorter CLUSTER_LIST length」。Step f)=BGP Identifier
  比較、g)=ピアアドレス比較なので、本文の「直後」は f)/g) 間に対応する。
- 対応: 本文が step 番号を書いていなかったため、RFC 4456 §9 の Step f)/g) 表現を
  各修正に明記して条文対応を追記。
- 情報源: RFC 4456 §9(原文で確認)。

### A6: `04_ipv6/05_transition_technologies.md` — DS-Lite MTU と MAP-E ポート計算 (完了日: 2026-07-13)

- 結果(a) DS-Lite MTU: RFC 6333 §5.3 は「B4 と AFTR の間のすべてのリンクの MTU を
  少なくとも 40 オクテット増やして、IPv6 カプセル化ヘッダと IPv4 データグラムを
  IPv6 を分割せずに収める」ことを **SHOULD** で推奨。ただし条文に「1540」という
  具体値はなく、1500 基準での帰結値。本文が「1540(RFC 6333 の推奨)」と
  具体値を RFC 由来のように書いていたので、条文どおり「40 増やす(SHOULD)」を
  主にし 1540 は帰結値と明示するよう精密化。
- 結果(b) MAP-E ポート計算: 正しい。a=6(オフセット)、k=8(PSID長)、m=16−6−8=2。
  Port = A×2^(16−a) + PSID×2^m + j = A×1024 + 52×4 + j = A×1024 + 208 + j。
  A=1 → 1232〜1235、A=2 → 2256〜2259、A=63 → 64720〜64723。A=1..63 の 63 か所 ×
  4 ポート = 252 ポート。EA-bits 0x1234 → IPv4 下位 0x12=18・PSID 0x34=52 も整合。
  本文の値・図・「252 ポート」すべて検算一致。修正不要。
- 対応: (a) のみ本文を精密化。(b) は修正なし。
- 情報源: RFC 6333 §5.3(原文で確認)、RFC 7597 のポートマッピング式(App.B の例に相当、
  自前で検算)。

### A7: `05_mpls_srv6/01_mpls_basics.md` — MPLS の ICMP トンネリング (完了日: 2026-07-13)

- 結果: 本文の記述は正確。RFC 3032 §2.3.2(Tunneling Private Addresses through a
  Public Backbone)原文「in order to send an ICMP message to the source of a packet,
  one can copy the label stack from the original packet to the ICMP message, and
  then label switch the ICMP message」「This will cause the message to proceed in
  the direction of the original packet's destination, rather than its source.
  Unless the message is label switched all the way to the destination host, it
  will end up, unlabeled, in a router which does know how to route to the source」。
- 対応: 本文に節番号 §2.3.2 を明記。「ラベルを積む」を「ラベルスタックをコピーして
  ラベルスイッチ」と原文寄りに、また出口で「経路を知るルータに届いてラベルが外れる」
  という条文どおりの説明に調整。
- 情報源: RFC 3032 §2.3.2(原文で確認)。

### A8: `05_mpls_srv6/02_ldp_rsvp_te.md` — LDP 能動側 / RSVP リフレッシュ (完了日: 2026-07-13)

- 結果(a) LDP: 正しい。RFC 5036 §2.5.2 原文「If A1 > A2, LSR1 plays the active role;
  otherwise, it is passive」— transport address の大きいほうが能動(TCP 646 へ接続)。
- 結果(b) RSVP: 正しい。RFC 2205 §3.7 の TIME_VALUES オブジェクトが運ぶリフレッシュ
  周期 R の既定は 30 秒。追加事実として、実際の送出は網内同期を避けるため
  [0.5R, 1.5R] の範囲でランダム化される(本文に補記)。
- 対応: (a)(b) とも節番号と原文/根拠を補記。RSVP はジッタ [0.5R,1.5R] の1文を追加。
- 情報源: RFC 5036 §2.5.2(原文で確認)、RFC 2205 §3.7(WebFetch 切れのため WebSearch で
  既定 30 秒・[0.5R,1.5R] を確認)。

---

## ステップ完了ログ

<!--
1ステップ完了するごとに、以下のフォーマットでこの下に追記していく(上書きしない)。

## Step N: <ファイルパス> (完了日: YYYY-MM-DD)

- 完了内容:
- 決定事項:
- 未解決・要検証事項:
- 次のステップ: Step N+1 <次のファイルパス>
-->

## Step 0: `shared/glossary.md`, `shared/style_guide.md` (完了日: 2026-07-05)

- 完了内容: 用語集と執筆スタイルガイドのひな形を作成。glossary.md はセクション分け
  (数字・A〜Z・日本語)と追記フォーマットを定義。style_guide.md は章構成テンプレート、
  RFC参照・略語・パケット図・章間参照などの表記ルールを実例付きで整備。
- 決定事項: (1) 用語集エントリは「定義/初出章/関連RFC/関連用語」の4項目形式。
  (2) パケットフォーマット図はRFC流のASCII図、メッセージ交換はMermaid sequenceDiagramを標準とする。
  (3) 本文は常体(〜である)。(4) 章間参照はファイルパスベースの相対リンク。
- 未解決・要検証事項: なし
- 次のステップ: Step 1 `00_overview/01_roadmap.md` から着手する

## Step 1: `00_overview/01_roadmap.md` (完了日: 2026-07-05)

- 完了内容: 教科書全体のロードマップを執筆。本書の目的と扱わない範囲、前提知識マップ(表形式)、
  5部構成の依存関係グラフ(Mermaid)、各部の章一覧、読み進め方の3モデル(標準/分野別/辞書的)を記載。
- 決定事項: (1) 大分類は「部」、個々の.mdファイルは「章」と呼ぶ。
  (2) 標準読了ルートは「第1部 → 第2部01〜04章 → 第3部 → 第2部05章(EVPN) → 第4部 → 第5部」。
  EVPNはMP-BGP依存のためBGPの後に回す。SRv6はIPv6依存。この2つのクロス依存を本文でも明示した。
- 未解決・要検証事項: なし
- 次のステップ: Step 2 `01_fundamentals/01_l2_l3_recap.md` から着手する

## Step 2: `01_fundamentals/01_l2_l3_recap.md` (完了日: 2026-07-05)

- 完了内容: L2/L3の役割分担とカプセル化の総復習章を執筆。フラット/階層アドレスの対比、
  カプセル化と次ヘッダ連鎖(EtherType/Protocol/Next Header)、透過的ブリッジング3動作、
  ホップバイホップ転送、Ethernet II/IPv4ヘッダフォーマット、異サブネット間の
  パケットウォークスルー(7ステップ)、層別トラブルシューティングを記載。
- 決定事項: (1) 伝送単位の呼称を厳密化: L2=フレーム、L3=パケット、L4=セグメント。
  (2) 「L3アドレスは末端間不変、L2アドレスは毎ホップ書き換え」を本書全体の基本イメージとして
  以後の章から参照する。(3) 用語集へ11語登録(ARP、EtherType、MTU、TTL、カプセル化、
  デフォルトゲートウェイ、透過的ブリッジング、フラッディング、フレーム/パケット/セグメント、
  ブロードキャストドメイン、ホップバイホップ転送、ロンゲストマッチ)。
- 未解決・要検証事項: なし
- 次のステップ: Step 3 `01_fundamentals/02_routing_table_basics.md` から着手する

## Step 3: `01_fundamentals/02_routing_table_basics.md` (完了日: 2026-07-05)

- 完了内容: ルーティングテーブルと経路選択の原理を執筆。経路選択を「①同一プロトコル内は
  メトリック → ②情報源間は管理距離 → ③転送時はロンゲストマッチ」の3段フィルタとして整理。
  CIDR(RFC 4632)、ECMP(RFC 2992)、RIB/FIB 分離、再帰的ルックアップ、経路集約と
  Null ルート、uRPF(RFC 3704)、基数木による LPM 実装、`ip route get` での切り分けを記載。
- 決定事項: (1) route の訳語は「経路」に統一(複合語の慣用名「デフォルトルート」等は除く)。
  glossary の経路集約エントリに表記ルールとして明記。
  (2) 「ルーティングテーブル」は RIB を指すと定義。(3) 管理距離は RFC 非標準の
  ベンダー慣例であることを本文で明示(値は Cisco 系慣例を代表として掲載)。
  (4) コントロールプレーン/データプレーンの対比を本章(RIB/FIB)で初導入し、
  以後 VXLAN・MPLS の章から参照する。(5) 用語集へ13語登録(CIDR、ECMP、FIB、RIB、
  Null ルート、uRPF、管理距離、経路集約、再帰的ルックアップ、直結経路、デフォルトルート、
  ネクストホップ、メトリック)。
- 未解決・要検証事項: 「2026年時点で IPv4 フルルート約100万経路」という数値は
  概数であり、公開統計(CIDR Report 等)での再確認が望ましい。
- 次のステップ: Step 4 `01_fundamentals/03_static_vs_dynamic.md` から着手する

## Step 4: `01_fundamentals/03_static_vs_dynamic.md` (完了日: 2026-07-05)

- 完了内容: 静的 vs 動的ルーティングの章を執筆。「静的=人間の意思、動的=ネットワークの現状を
  テーブルに反映する」という対比を軸に、静的の2つの限界(1ホップ先までしか見えない/規模で破綻)、
  動的のコスト(資源・複雑さ・伝聞リスク)、使い分けの定石(中核は動的・単一接続末端は静的+集約)、
  フローティングスタティック、動的プロトコル共通の5段階ライフサイクル、障害検知の3階層
  (物理層/タイマー/BFD、RFC 5880)、再配送とそのリスクを記載。
- 決定事項: (1) 収束(convergence)を本章で正式導入し、「プロトコル設計の中心的評価軸」として
  以後の章(特に 04_distance_vector_link_state)から参照する。(2) DV/LS の分類と IGP/EGP の分類は
  本章では見取り図のみ提示し、詳細はそれぞれ 04章・05章に委ねる構成とした(AS の用語定義も未導入、
  05章で導入予定)。(3) ブラックホール(故障モード)と Null ルート(意図的廃棄)を区別する用語として
  定義。(4) 用語集へ8語登録(BFD、再配送、収束、静的経路、動的ルーティング、ネイバー、
  ブラックホール、フローティングスタティックルート)。静的経路・動的ルーティングの初出章は
  02章だが主題章は03章である旨を併記した。
- 未解決・要検証事項: 「隣接(アジャセンシー)/ピア」の呼称は本章では括弧書きで触れるに
  とどめた。OSPF 章(05_igp_overview または第3部)で正式に用語集登録する。
- 次のステップ: Step 5 `01_fundamentals/04_distance_vector_link_state.md` から着手する

## Step 5: `01_fundamentals/04_distance_vector_link_state.md` (完了日: 2026-07-05)

- 完了内容: ディスタンスベクタ vs リンクステートの章を執筆。「何を教え合うか
  (距離=計算結果 vs リンク状態=一次情報)」を軸に、分散ベルマンフォードと
  無限カウント+対症療法(距離上限16・スプリットホライズン・ポイズンリバース・
  トリガードアップデート)、リンクステートのフラッディング規律(シーケンス番号・ACK・
  エージング)と LSDB 同期・SPF、「伝搬と計算の分離」による収束の違い、
  マイクロループと SPF スロットリング、パスベクタ(BGP)の予告、比較表を記載。
  RFC 1058/2453(RIP)、RFC 2328(OSPFv2)、RFC 1195(IS-IS)を引用。
- 決定事項: (1) LSA という語は「OSPF での呼称」として言及のみにとどめ、正式な定義・
  用語集登録は次章以降(OSPF を扱う章)に委ねた。LSDB・SPF は方式一般の用語として
  本章で登録。(2) 「フラッディング」は L2(01章初出)とリンクステートで同語異用途のため、
  glossary の既存エントリを両用法併記に更新した。(3) パスベクタを用語集登録し、
  詳細は第3部へ委ねる旨を明記。(4) RIP のタイマー値は RFC 2453 準拠
  (update 30s / timeout 180s / garbage collection 120s)で記載。
  (5) 用語集へ7語登録(LSDB、SPF、スプリットホライズン、ディスタンスベクタ、
  パスベクタ、無限カウント、リンクステート)+フラッディングの定義更新。
- 未解決・要検証事項: (1) マイクロループ対策の標準化動向(RFC 8333 uloop delay 等)には
  触れていない。IGP 章か MPLS/SR 章で扱うか要検討。(2) 「隣接が Full まで進んでいるか」等
  OSPF の状態名をトラブルシューティングで先出ししたが、正式な定義は次章で行う
  (Step 4 からの持ち越し事項と合わせて次章で解消予定)。
- 次のステップ: Step 6 `01_fundamentals/05_igp_overview.md` から着手する

## Step 6: `01_fundamentals/05_igp_overview.md` (完了日: 2026-07-05)

- 完了内容: IGP の位置づけと BGP への橋渡しの章を執筆(第1部完結)。AS(RFC 1930)と
  IGP/EGP の2階建て構造を「内は地図で速く、外は方針で堅く」という要求の違いから導入。
  OSPF(RFC 2328)を「素朴なリンクステートを実務規模で動かす3つの工夫」
  (ネイバー/隣接の分離、DR/BDR、エリア)として整理。パケット5種と共通ヘッダ、
  隣接ステートマシン(Down〜Full)、LSA タイプ(1/2/3/4/5)、
  「エリア間は実質ディスタンスベクタでありエリア0経由の強制でループを防ぐ」という洞察、
  IGP=土台(ループバック+インフラ)/BGP=積み荷という実務の分業と
  再帰的ルックアップによる BGP ネクストホップ解決を記載。
- 決定事項: (1) 前章までの持ち越し事項をすべて解消: 隣接(アジャセンシー)/ピアを正式に
  用語集登録(ネイバー⊃隣接の関係、ピアは BGP の呼称)、LSA を正式定義・登録、
  OSPF の状態名(Full 等)を正式導入、AS を用語集登録。
  (2) IS-IS は「OSPF の方言」として位置づけ、要所の比較にとどめる(専用章は設けない)。
  (3) マイクロループ対策の標準化動向(RFC 8333 等)は第5部(MPLS/SR、TI-LFA の文脈)で
  扱うこととし、本章では触れない。(4) IGP/BGP の分業(IGP はループバックとインフラのみ)は
  第3部・第2部(EVPN アンダーレイ)・第5部から参照する基本構図とする。
  (5) 用語集へ11語登録(AS、DR/BDR、EGP、IGP、IS-IS、LSA、OSPF、エリア、ピア、
  隣接、ルータID)+ネイバーの定義を隣接/ピアとの関係で更新。
- 未解決・要検証事項: FRRouting の `show ip ospf neighbor` 出力例の列見出し・表記は
  代表的な形で記載した(バージョンにより微差がありうる。動作原理の説明には影響なし)。
- 次のステップ: Step 7 `02_vlan_vxlan_evpn/01_vlan_basics.md` から着手する(第2部開始)

## Step 7: `02_vlan_vxlan_evpn/01_vlan_basics.md` (完了日: 2026-07-05)

- 完了内容: 第2部の起点となる VLAN 基礎の章を執筆。ブロードキャストドメイン分割の
  3つの動機(ブロードキャスト氾濫・ポリシー境界・物理と論理の癒着)から導入し、
  IEEE 802.1Q の位置づけ、タグが必要な理由(1リンクに複数VLANを混在させるため)、
  IVL による (VLAN, MAC) キーの学習分離、802.1Q タグフォーマット(TPID/PCP/DEI/VID)、
  予約 VID(0/4095)、フレーム長 1518→1522、アクセスポートと PVID の入出方向対称性、
  フレームウォークスルー3ケース、Linux bridge (vlan_filtering) での設定例、
  トラブルシューティング(VLAN不一致、NICオフロードでタグが見えない罠)を記載。
- 決定事項: (1) 章の分担: タグのフォーマット自体は本章、タグ付きリンクの運用
  (トランク、ネイティブVLAN)は次章 02 に委ねる。VLAN間ルーティング(SVI /
  router-on-a-stick)は専用章がないため本章で簡潔に扱った。
  (2) 「1 VLAN = 1 ブロードキャストドメイン = 1 IPサブネット」を第2部全体の
  設計の基本文法として導入。(3) 「VID 12ビット = 4094 上限」を VXLAN 章
  (03_vxlan_fundamentals)への伏線として明示。「タグ/カプセル化はフレームを太らせる」
  も VXLAN・MPLS への伏線として記載。(4) IEEE 規格の参照ルール(年版表記、
  802.1D→802.1Q 統合の扱い)を style_guide.md の実例ログへ初登録。
  (5) 用語集へ6語登録(802.1Q、IVL/SVL、PVID、SVI、VLAN、アクセスポート)。
- 未解決・要検証事項: (1) DEI の再定義(CFI→DEI)の正確な版(802.1Q-2011 とされるが
  本文では「現行仕様で再定義」とのみ記載、要確認)。(2) トランクポートの用語集登録は
  次章で行う(本章では意図的に「タグ付きリンク」と呼び、トランクの語は使っていない)。
- 次のステップ: Step 8 `02_vlan_vxlan_evpn/02_trunking_native_vlan.md` から着手する

## Step 8: `02_vlan_vxlan_evpn/02_trunking_native_vlan.md` (完了日: 2026-07-05)

- 完了内容: トランキングとネイティブ VLAN の章を執筆。802.1Q の形式モデル
  (VLAN ごとのメンバーシップ+出方向タグ規則+PVID)を先に提示し、アクセス/トランクを
  その設定パターンの慣用名として統一的に説明。トランク上の MAC 学習(対向の MAC が
  1ポートに集まる見え方、VXLAN への伏線)、ネイティブ VLAN の由来(後方互換・
  タグなし制御フレーム)と定石(未使用 VID / 全タグ付き)、許可 VLAN リスト=
  ブロードキャストドメインの延長範囲、入方向の2段規則(分類→ingress filtering)、
  ネイティブ VLAN 不一致による混線、二重タグ VLAN ホッピングと対策、
  802.1ad(QinQ、S-TAG 0x88A8)と「L2 の学習・フラッディング自体が規模の壁」
  という VXLAN への動機づけ、Linux bridge でのトランク設定例、
  tcpdump の `vlan` キーワードのオフセットの罠を記載。
- 決定事項: (1) Step 7 持ち越しを解消: トランクポート・ネイティブ VLAN を用語集に
  正式登録。DEI の経緯は「802.1ad(2005)の S-TAG で導入 → 802.1Q-2011 で C-TAG の
  CFI を DEI に再定義」と本文に記載。(2) 仕様に存在しない業界慣用名は初出時に
  仕様上の正確な言い方と由来を併記する、というルールを style_guide.md 実例ログに登録。
  (3) STP は本書に専用章がないため、BPDU がタグなしで流れる事実の言及にとどめた。
  (4) QinQ の限界(中継網の MAC 学習・フラッディング)を第2部の縦糸
  「識別子の数ではなく L2 の動作原理が壁」として明示し、03 章(VXLAN)の導入に接続する。
  (5) 用語集へ3語登録(802.1ad、トランクポート、ネイティブ VLAN)+
  802.1Q・アクセスポートの関連用語を更新。
- 未解決・要検証事項: (1) STP を本書でどこまで扱うか(専用章なし。第2部内で
  コラム的に補うか、割り切って対象外と明記するか)は未決定。
  (2) tcpdump の `vlan` フィルタの挙動はカーネル/libpcap のバージョンや
  オフロード状態に依存する面がある(原理説明としては問題ないが、
  環境により再現が異なりうる旨は本文に含めていない)。
- 次のステップ: Step 9 `02_vlan_vxlan_evpn/03_vxlan_fundamentals.md` から着手する

## Step 9: `02_vlan_vxlan_evpn/03_vxlan_fundamentals.md` (完了日: 2026-07-06)

- 完了内容: VXLAN の基礎の章を執筆(RFC 7348)。前章の結論「L2 の学習・フラッディング
  原理が壁」から接続し、DC の相反する2要求(端末には L2 連続性/物理網は L3)→
  オーバーレイ/アンダーレイの分離として導入。MAC-in-UDP カプセル化スタック、
  UDP を使う理由(内側フローのハッシュを外側送信元ポートに載せてアンダーレイ ECMP を
  活かす)、VNI 24 ビット(本質は識別子の数ではなく転送原理の置き換え、VLAN との
  対比表)、フラッド&ラーン(内側送信元 MAC ↔ 外側送信元 VTEP のデータプレーン学習、
  BUM のマルチキャスト配送)、VXLAN ヘッダ ASCII 図、オーバーヘッド 50 オクテットと
  MTU 設計、ARP 解決〜既知ユニキャストのウォークスルー(Mermaid)、
  「MAC テーブルの転送先が IP になる」見え方、Linux vxlan デバイスの設定例と
  FDB の読み方、トラブルシューティング4種(MTU/PMTUD ブラックホール、
  ポート 4789 vs Linux デフォルト 8472、BUM だけ死ぬ=マルチキャスト起因、
  トンネル区間キャプチャの解釈)を記載。
- 決定事項: (1) 章の分担: 本章はカプセル化フォーマットとデータプレーン
  (フラッド&ラーン)まで。コントロールプレーンの比較(マルチキャスト vs EVPN、
  ヘッドエンドレプリケーションの詳細)は次章 04 に委ねる(HER は本章で言及のみ、
  用語集登録も次章)。(2) 第1部からの参照を明示的に張った: ECMP(UDP 送信元ポートの
  設計理由)、コントロールプレーン/データプレーン(フラッド&ラーンは「コントロール
  プレーンなし」と位置づけ)、ループバック定石(VTEP アドレス)、カプセル化原則。
  (3) 表記: 「オーバーレイ」「アンダーレイ」(ネットワークを付けた形を用語集見出しに)、
  「フラッド&ラーン」を標準表記とする。(4) RFC 7348 が Informational である旨を
  本文に明記するスタイルを採った。(5) 前章までの「(後述: 03_...)」参照3箇所を
  通常リンクに更新。(6) 用語集へ7語登録(BUM トラフィック、VNI、VTEP、VXLAN、
  アンダーレイネットワーク、オーバーレイネットワーク、フラッド&ラーン)+
  VLAN の関連用語に VNI を追加。
- 未解決・要検証事項: (1) 古い tcpdump での VXLAN デコードに関する記述
  (「`-T vxlan` の指定が必要な場合がある」)はバージョン依存のため要検証。
  (2) リーフ・スパイン(Clos)構成は導入で一言触れたのみ。どこかで図解を入れるか
  次章以降で要検討。(3) VXLAN のゲートウェイ(VNI 間・VXLAN⇔VLAN の L3 境界)は
  本章では扱っていない。EVPN の章(05)の IRB の文脈で扱う想定。
- 次のステップ: Step 10 `02_vlan_vxlan_evpn/04_vxlan_control_plane.md` から着手する

## Step 10: `02_vlan_vxlan_evpn/04_vxlan_control_plane.md` (完了日: 2026-07-06)

- 完了内容: VXLAN のコントロールプレーン比較の章を執筆。VTEP の知識を
  「問い A: BUM の届け先(メンバーシップ)/問い B: MAC の所在」の2問に帰着させ、
  3方式(①アンダーレイマルチキャスト=IGMP/PIM-SM の動作・RP・VNI↔グループ設計と
  運用の重さ、②ヘッドエンドレプリケーション=静的リストの O(N²) 更新問題、
  ③EVPN=RT-2/RT-3 による事前配布と自動構築、ARP サプレッション)を比較。
  第1部の「静的 vs 動的」の構図の再演として全体を組み立てた。EVPN での ARP の
  シーケンス図(フラッド&ラーンとの対比)、3方式比較表、Linux での静的 HER 設定例
  (「EVPN が自動化するものの手動版」)、トラブルシューティング4種(IGMP クエリア
  不在の時限式障害、HER リスト更新漏れの非対称障害、方式混在の片方向不全、
  EVPN 移行後の学習2系統問題)を記載。RFC 7432/8365/4760/3376/7761/5015 を引用。
- 決定事項: (1) 章の分担: 本章は「なぜコントロールプレーンが必要で、なぜ BGP か」
  という方式選択の理解まで。EVPN の内部(全ルートタイプ、MAC モビリティ、IRB)は
  次章 05 に委ねる。BGP/MP-BGP は本章ではブラックボックス(「型付きの情報を
  ポリシー制御つきで大規模配布する完成した機構」)として扱う(読了順で第3部より
  先に読まれるため)。(2) Step 9 持ち越しの HER を用語集に正式登録。
  (3) 本書では以後、断りがなければ EVPN+HER(BUM はユニキャスト複製、リストは
  RT-3)の構成を前提とする旨を本文の比較表の注で宣言。(4) 表記: 「ヘッドエンド
  レプリケーション(HER)」を標準表記とし ingress replication を併記。
  「ルートタイプ2/RT-2」の略記を導入。(5) 用語集へ6語登録(ARP サプレッション、
  EVPN、IGMP、MP-BGP、PIM、ヘッドエンドレプリケーション)+ VXLAN・
  フラッド&ラーンの関連用語を更新。(6) 前章の「(後述: 04_...)」参照2箇所を
  通常リンクに更新。
- 未解決・要検証事項: (1) ARP サプレッションの用語集エントリで RFC 9161
  (Proxy-ARP/ND in EVPN)を関連 RFC に挙げたが、本文では引用していない。
  次章(05)で扱うか要検討。(2) 「大規模 DC では IGP の代わりに BGP で
  アンダーレイを組む設計もある」(RFC 7938 の話)は一言触れたのみ。
  第3部 06_large_scale_design で扱う想定。(3) リーフ・スパイン構成の図解は
  本章でも入れていない(Step 9 からの持ち越し)。次章 05 の実務構成の文脈で
  入れるのが自然か。
- 次のステップ: Step 11 `02_vlan_vxlan_evpn/05_evpn_vxlan.md` から着手する

## Step 11: `02_vlan_vxlan_evpn/05_evpn_vxlan.md` (完了日: 2026-07-06)

- 完了内容: EVPN/VXLAN の統合的理解の章を執筆(第2部完結)。前章の「2つの問い」の先に
  残る4つの問い(マルチテナンシー/L3/モビリティ/冗長化)を導入の軸に、
  リーフ・スパイン構成の図解(Step 9〜10 からの持ち越しを解消)、ルートタイプ全体像
  (RT-1〜5 の表、RFC 9251 は言及のみ)、RD(一意化)と RT(仕分け)・MAC-VRF・
  RT 自動導出(AS:VNI)、IRB(RFC 9135、エニーキャストゲートウェイ、対称/非対称、
  L3VNI、Router's MAC)、RT-5(RFC 9136)、MAC モビリティ(シーケンス番号、
  重複 MAC 検出 180秒/5回)、マルチホーミング概観(ESI、RT-1/RT-4、DF、
  マスウィズドロー、local bias は言及)、RT-2/RT-3 の NLRI フォーマット図、
  PMSI トンネル属性(RFC 6514)、ウォークスルー2本(VM 起動→FDB 搭載、
  対称 IRB のサブネット間通信)、FRR の設定例と `show bgp l2vpn evpn`・
  `extern_learn` 付き FDB の読み方、トラブルシューティング4種(RT 不一致、
  IRB 構成要素の欠落・方式混在、重複 MAC 検出の凍結、VNI マッピング不整合)を記載。
- 決定事項: (1) 表記の使い分けを宣言: 「RT-数字」=ルートタイプ、単独「RT」=
  ルートターゲット(glossary 両エントリに明記)。(2) 本書は VLAN-based サービス
  (1 MAC-VRF = 1 VNI)と対称 IRB を以後の前提とする。(3) マルチホーミングは
  概観にとどめ、詳細設計(DF 選出アルゴリズム、local bias の動作)は本書の
  範囲外と本文で明示。(4) VLAN-aware bundle・EVPN マルチキャスト(RT-6〜8)は
  存在の言及のみ。(5) 本章は第3部読了後に読む前提(ロードマップの読了順)で
  BGP 用語(ルートリフレクタ、拡張コミュニティ)を「(後述: 第3部)」参照で使用。
  (6) Step 10 持ち越しの RFC 9161 を ARP/ND 代理応答の運用詳細として本文で引用。
  (7) 用語集へ10語登録(ESI、IRB、L2VNI/L3VNI、MAC-VRF、MAC モビリティ、RD、RT、
  エニーキャストゲートウェイ、リーフ・スパイン、ルートタイプ)+ EVPN の関連用語更新。
  (8) 前章 04 の「(後述: 05_evpn_vxlan.md)」参照をリンク化。
- 未解決・要検証事項: (1) FRR の `show bgp l2vpn evpn` 出力・`autort
  rfc8365-compatible` オプションはバージョンにより表示・挙動に差がありうる
  (動作原理の説明には影響なし)。(2) 重複 MAC 検出後の凍結解除の挙動
  (タイマー/手動クリア)は実装差が大きい旨を本文でぼかして記載した。
  (3) 「大規模 DC の eBGP アンダーレイ設計(RFC 7938)」は引き続き
  第3部 06_large_scale_design で扱う想定(Step 10 から継続)。
  (4) 本章内の第3部への参照3箇所(05_mp_bgp、04_policy_control、
  06_large_scale_design)は「(後述: ...)」形式のため、該当章の執筆後にリンク化する。
- 次のステップ: Step 12 `03_bgp/01_bgp_basics.md` から着手する(第3部開始)

## Step 12: `03_bgp/01_bgp_basics.md` (完了日: 2026-07-06)

- 完了内容: 第3部の起点となる BGP 基礎の章を執筆(RFC 4271)。IGP を AS 間に
  持ち出せない3つの理由(信頼・ポリシー・規模)から導入し、BGP の設計を
  「4つの選択」(① TCP ポート 179 への分業と順序保証、② ピアの明示設定=
  境界の安全装置、③ AS_PATH によるパスベクタ=第1部04の伏線回収、
  ④ 差分更新・ハードステートと Route Refresh RFC 2918)として整理。
  3段の RIB(Adj-RIB-In / Loc-RIB / Adj-RIB-Out)とポリシーの関所、
  共通ヘッダ・4メッセージ(OPEN のフィールドとケイパビリティ交渉 RFC 5492、
  AS_TRANS、UPDATE の構造= NLRI と撤回リスト、NOTIFICATION、KEEPALIVE)、
  FSM 6状態(Active の名前の罠、コリジョン解決)、タイマー(ホールドタイム
  の min 合意、推奨90秒/実装既定180秒)、セッション確立の Mermaid 図、
  FRR の最小 eBGP 設定と `show bgp summary` の読み方、トラブルシューティング
  4種(Active 巡回= TCP 179 疎通、OPEN 拒絶= NOTIFICATION を読む、
  ネクストホップ解決不能、PMTUD 起因の Hold Timer Expired)を記載。
- 決定事項: (1) 章の分担: 本章はセッション・メッセージ・FSM という
  eBGP/iBGP 共通の土台まで。eBGP/iBGP の違い(TTL、スプリットホライズン等)は
  02章、パスアトリビュートの意味論と Decision Process の各段は 03章、
  ポリシー制御の道具は 04章に委ねる(本文でその旨を明示)。
  (2) BGP の経路選択は「メトリック最小化ではなく多段の属性比較+ポリシー」と
  位置づけ、本章では比較の中身に立ち入らない。(3) eBGP の TTL=1 は
  トラブルシューティングで言及のみ(詳細は02章)。(4) 用語集へ5語登録
  (AS_PATH、Adj-RIB-In / Loc-RIB / Adj-RIB-Out、BGP、NLRI、ホールドタイム)+
  ピア・パスベクタの「詳細は第3部」参照を本章へのリンクに更新。
- 未解決・要検証事項: (1) ホールドタイムの実装既定値「180秒(KEEPALIVE 60秒)が
  多い」は代表的実装(FRR、Cisco IOS 等)の慣例として記載(RFC 4271 Section 10 の
  推奨値は Hold 90s / Keepalive 30s / ConnectRetry 120s)。
  (2) `show bgp summary` の出力例は FRR の代表的な形(バージョンにより列に微差)。
  (3) 第2部 05章の「(後述: 第3部)」参照3箇所(05_mp_bgp、04_policy_control、
  06_large_scale_design)は本章の対象外のため未解消(該当章の執筆時にリンク化)。
- 次のステップ: Step 13 `03_bgp/02_ibgp_ebgp.md` から着手する

## Step 13: `03_bgp/02_ibgp_ebgp.md` (完了日: 2026-07-06)

- 完了内容: iBGP/eBGP の違いの章を執筆。「同じプロトコル、2つの規則」を軸に、
  トランジット AS の内部運搬問題(IGP 再配送が規模と情報喪失で破綻)から iBGP を導入。
  eBGP の規則(AS_PATH 前置、NEXT_HOP 書き換え、TTL 1 慣例と GTSM RFC 5082)、
  iBGP の規則(両方とも変更しない → AS_PATH ループ検出の穴 → 再広告禁止
  RFC 4271 Section 9.2 → フルメッシュ n(n-1)/2)、対比一覧表(LOCAL_PREF/MED の
  スコープ、管理距離 20/200 は Cisco 系慣例と明記)、ネクストホップ問題と
  next-hop-self、ループバックピアリング + update-source(IGP への積層関係)、
  トランジット通過のウォークスルー(Mermaid + 区間別の変化表)、
  中継ルータ P の問題(同期ルールの歴史、全員 iBGP か MPLS かの二択)、
  FRR 設定例(eBGP/iBGP の対比、`*>i` 行の読み方)、トラブルシューティング4種
  (マルチホップ TTL / GTSM 片側有効、update-source 漏れの RST、
  フルメッシュ欠け=規則③の正常動作、next-hop-self 漏れの inaccessible)を記載。
- 決定事項: (1) 「iBGP スプリットホライズン」は慣用名であり RFC 4271 に登場しない旨を
  本文に明記(慣用名ルールの適用)。(2) 同期(synchronization)は「RFC 4271 の仕様に
  含まれない歴史的な運用規則」として1段落のみ。(3) ルートリフレクタ/
  コンフェデレーションは存在の言及と RFC 番号(4456/5065)のみで 06章に委ねる
  (用語集登録も 06章で行う)。(4) BGP フリーコアは P の問題の解決策②として
  言及し、詳細は第5部へ(「(後述: ../05_mpls_srv6/01_mpls_basics.md)」参照)。
  (5) 管理距離の章間一貫性: 第1部02 と同様「RFC 非標準のベンダー慣例」と明記し、
  Juniper 系は eBGP/iBGP を区別しない(170)ことを併記。(6) 用語集へ4語登録
  (eBGP / iBGP、GTSM、ネクストホップセルフ、フルメッシュ)+ BGP の関連用語を更新。
  (7) 前章 01 の「(後述: 02_ibgp_ebgp.md)」参照2箇所をリンク化。
- 未解決・要検証事項: (1) eBGP の TTL 1 の扱い(送信時に 1 を設定するか、
  受信時に検査するか)は実装差があるため「多くの実装は〜」とぼかして記載。
  (2) FRR の inaccessible 表示・`show bgp` の列構成はバージョンにより微差がありうる。
  (3) 第三者ネクストホップ(third-party next hop)は括弧書きの言及のみ
  (深入りは意図的に回避)。(4) 本章から 06_large_scale_design・
  03_path_attributes・05_mpls_srv6/01 への「(後述: ...)」参照は該当章の執筆後に
  リンク化する。
- 次のステップ: Step 14 `03_bgp/03_path_attributes.md` から着手する

## Step 14: `03_bgp/03_path_attributes.md` (完了日: 2026-07-06)

- 完了内容: パスアトリビュートの章を執筆。「経路=プレフィックス+属性の束(履歴書)」を軸に、
  TLV フォーマットとフラグ(Optional/Transitive/Partial/Extended Length)、4分類と
  「未知属性の扱い」による後方互換な拡張性(AS4_PATH を実例に)、属性カタログ
  (ORIGIN、AS_PATH の AS_SEQUENCE/AS_SET と RFC 6472、NEXT_HOP、LOCAL_PREF、MED、
  ATOMIC_AGGREGATE/AGGREGATOR)、スコープの設計(意思は AS 内・希望は隣まで・
  事実は全世界)、経路選択プロセス(RFC 4271 Section 9.1、3フェーズ+多段比較の表)、
  ホットポテトルーティング、実装拡張(Weight・古い経路の優先)、ウォークスルー
  (マルチホームで AS_PATH 長 vs LOCAL_PREF の2つの結末)、入口制御の4手段
  (広告停止 > MED > プリペンド > コミュニティ依頼)、FRR の bestpath 理由表示の
  読み方、トラブルシューティング4種(プリペンド不発=相手の LOCAL_PREF、
  MED 不発=same-AS 制約と実装ノブ、選択の食い違い=oldest/router-id、
  属性エラーと RFC 7606 treat-as-withdraw)を記載。
- 決定事項: (1) 表記: 「経路選択プロセス(Decision Process)」を標準表記として
  用語集登録。(2) 章の分担: 属性の意味論と比較順序は本章、属性を書き換える道具
  (route-map、コミュニティ)は 04章、MP_REACH_NLRI は 05章、
  ORIGINATOR_ID/CLUSTER_LIST は 06章に委ねる(本文で明示)。COMMUNITY 類の
  用語集登録も 04章で行う。(3) Weight・oldest route・MED の解釈ノブは
  「RFC 外の実装慣例」と明記(管理距離と同じ扱いのルール)。
  (4) 「出口は命令できるが入口は頼むしかない」「トラフィックは広告と逆向きに流れる」を
  第3部のポリシー章(04)へ接続する基本構図として導入。(5) 用語集へ6語登録
  (AS_PATH プリペンド、LOCAL_PREF、MED、ORIGIN、経路選択プロセス、
  パスアトリビュート)+ AS_PATH の関連用語を更新。(6) 01章・02章の
  「(後述: 03_path_attributes.md)」参照3箇所をリンク化。
- 未解決・要検証事項: (1) 「MED なしは最小値 0(最優先)として扱う」は RFC 4271
  Section 9.1.2.2 の規定として記載したが、条文の正確な文言は要再確認
  (RFC 1771 時代は未規定で実装差の源だった経緯も含め)。(2) FRR の
  `show bgp ipv4 unicast` の bestpath 理由表示(best (AS Path) 等)は
  バージョンにより文言に微差がありうる。(3) 属性エラー起因の広域障害の実例
  (2010年前後の事案)は特定の事件名を挙げず「複数回発生している」とのみ記載。
  (4) 本章から 04_policy_control・05_mp_bgp・06_large_scale_design への
  「(後述: ...)」参照は該当章の執筆後にリンク化する。
- 次のステップ: Step 15 `03_bgp/04_policy_control.md` から着手する

## Step 15: `03_bgp/04_policy_control.md` (完了日: 2026-07-06)

- 完了内容: ポリシー制御の章を執筆。ポリシーを「どこで(ピア・方向)・どの経路に
  (マッチ)・何をするか(アクション)」に分解し、3段 RIB 上の2つの関所
  (入力/出力ポリシー)と対応付けた。prefix-list(ge/le の2条件 AND の意味論、
  代表4パターンの表)、AS_PATH 正規表現(`_`・`^$` の最小語彙)、コミュニティ
  (RFC 1997、情報タグ/アクション依頼の2用法、well-known 5種の表)、
  拡張コミュニティ(RFC 4360、RT の正体)、ラージコミュニティ(RFC 8092)、
  route-map(先勝ち+暗黙の deny、受け皿 permit、2層の permit/deny)、
  RFC 8212(eBGP default deny)と経路リーク(RFC 7908)、ポリシーの再評価
  (Route Refresh RFC 2918 / soft reconfiguration、01章の伏線回収)、
  商業関係(顧客/ピア/上流)に基づくポリシーのウォークスルー
  (入口タグ+出口選別、valley-free、LOCAL_PREF 序列の経済的導出)、
  GRACEFUL_SHUTDOWN(RFC 8326)と BLACKHOLE(RFC 7999、RTBH)の動作、
  FRR 設定例(顧客 in / 上流 out)、トラブルシューティング4種
  (暗黙の deny と RFC 8212、send-community 既定差と第三者の洗い流し、
  再評価のタイミング、ge/le の解釈ミス)を記載。
- 決定事項: (1) 表記: 「prefix-list」「route-map」は実装語彙に合わせ英字のまま
  標準表記とする(glossary に明記)。route-map は RFC に存在しない実装語彙である旨を
  本文に明記(慣用名ルールの適用)。(2) 章の分担: RPKI / Origin Validation
  (RFC 6811)は言及のみで本書の範囲外と明示。NO_EXPORT_SUBCONFED は
  06章への「(後述)」参照。RPSL(RFC 2622)は存在の言及のみ。
  (3) 「先勝ち+暗黙の deny」を prefix-list / route-map 共通の評価規則として
  導入し、RFC 8212 の default deny と同じフェイルセーフ原則で括った。
  (4) 商業関係ポリシー(入口でタグ・出口でタグを見る)を第3部後半
  (06_large_scale_design)から参照できる実務の基本形として導入。
  (5) 用語集へ6語登録(prefix-list、route-map、拡張コミュニティ、経路リーク、
  コミュニティ、ラージコミュニティ)+ RT(初出章欄・関連用語)と
  パスアトリビュート(関連用語)を更新。(6) 01章・03章・第2部05章の
  「(後述: 04_policy_control)」参照3箇所をすべてリンク化。
- 未解決・要検証事項: (1) match 句が参照する prefix-list / community-list が
  未定義・空の場合の挙動は「実装差がある」とぼかして記載(FRR の現行の
  正確な挙動は要検証)。(2) FRR のコミュニティ送信の既定(standard / extended /
  large とも既定送信)はバージョンにより差がありうる。(3) `show bgp ipv4 unicast`
  の Community 行・best 理由の表示文言は FRR の代表的な形で記載。
  (4) 本章から 06_large_scale_design への「(後述: ...)」参照
  (NO_EXPORT_SUBCONFED)は該当章の執筆後にリンク化する。
- 次のステップ: Step 16 `03_bgp/05_mp_bgp.md` から着手する

## Step 16: `03_bgp/05_mp_bgp.md` (完了日: 2026-07-06)

- 完了内容: MP-BGP の章を執筆(RFC 4760)。「BGP の本体一覧に IPv4 が登場しない」
  という棚卸しから導入し、BGP-4 で IPv4 が焼き付いていた3箇所(NLRI・撤回リスト・
  NEXT_HOP)→ MP_REACH_NLRI / MP_UNREACH_NLRI(Type 14/15)への移設、
  optional non-transitive の選択理由(AS4_PATH との対比: 到達性情報は「処理への
  参加」を要するため運び屋にさせない)、AFI/SAFI の型システム(主要ファミリ表、
  EVPN のルートタイプは入れ子の型)、ケイパビリティ交渉(RFC 5492、積集合、
  OPEN でしか交換できない→後付けはセッションリセット)、セッション多重の構造
  (RIB・経路選択・ポリシー・Route Refresh はファミリごと、生死はセッションで
  運命共同体)、fate sharing の設計論(AS 間はファミリとセッションを揃える/
  オーバーレイ系はループバック多重)、両属性のフォーマット図、UPDATE の新旧対比図、
  IPv6 ネクストホップの2個併記(RFC 2545)と RFC 8950、FRR の address-family
  2層構造の設定例、トラブルシューティング4種(activate 漏れ=静かな失敗、
  ネクストホップ解決不能2態、後付けリセット、RT 不一致は MP-BGP の上の層)を記載。
- 決定事項: (1) 切り分けの層構造「①セッション→②ファミリ→③ポリシー→
  ④取り込み(RT)→⑤ネクストホップ」を本章で定式化(第2部 05 章のトラブル
  シューティングと接続)。(2) 章の分担: VPN-IPv4(SAFI 128)・ラベル付き
  ユニキャスト(SAFI 4)は表での位置づけのみで第5部へ、IPv6 ユニキャストの
  実践(RFC 2545 のリンクローカル併記の理由= NDP)は第4部へ委ねる。
  Flowspec(RFC 8955)・BGP-LS(RFC 9552)は存在の言及のみ。
  (3) Dynamic Capability は「標準化・普及に至っていない」とし、
  「ファミリの後付けはリセットを伴う」を本書の原則として記載。
  (4) 用語集へ3語登録(AFI / SAFI、MP_REACH_NLRI / MP_UNREACH_NLRI、
  ケイパビリティ広告)+ MP-BGP の定義を更新(「詳細は第3部で扱う」を削除し
  実体の記述を追加)。(5) 「(後述: 05_mp_bgp.md)」参照4箇所
  (01章1、03章2、第2部05章1)をすべてリンク化。
- 未解決・要検証事項: (1) 「ケイパビリティなしの OPEN は IPv4 ユニキャスト
  のみ有効とみなす」は伝統的挙動として記載(RFC 4760/5492 の条文上の根拠は
  暗黙的で、実装差がありうる)。(2) address-family 追加時の挙動(NOTIFICATION
  の Cease で張り直し)は代表的実装の動作として記載(実装により Route Refresh
  併用の部分緩和あり、と併記)。(3) FRR の `bgp default ipv4-unicast` の既定は
  バージョンにより異なりうる旨を本文に明記済み。(4) RFC 4760 冒頭の
  「IPv4 固有は3箇所(NEXT_HOP・AGGREGATOR・NLRI)」の引用は要旨の意訳
  (条文の正確な文言は要再確認。AGGREGATOR は拡張不要とした整理も RFC の
  趣旨に基づく本書の言い換え)。
- 次のステップ: Step 17 `03_bgp/06_large_scale_design.md` から着手する

## Step 17: `03_bgp/06_large_scale_design.md` (完了日: 2026-07-07)

- 完了内容: 大規模設計の章を執筆(第3部完結)。iBGP フルメッシュの n(n-1)/2 の破綻から、
  「再広告禁止=ループ防止だから、緩めるなら代替のループ検出が要る」→ 2つの解はどちらも
  パスベクタ原理の AS 内への再輸入、という軸で構成。ルートリフレクタ(RFC 4456、反射3規則、
  クライアント無改造による漸進導入、ORIGINATOR_ID/CLUSTER_LIST、経路選択への2つの修正、
  パスハイディング=経路多様性喪失+ホットポテトの歪みと ADD-PATH RFC 7911 言及、
  冗長 RR とクラスタ ID の2流儀、階層化)、コンフェデレーション(RFC 5065、
  AS_CONFED_SEQUENCE/SET、LOCAL_PREF/NEXT_HOP/MED 保持、パス長に数えない、境界での除去、
  NO_EXPORT_SUBCONFED の伏線回収)、RR vs confed 比較表、DC 設計(RFC 7938 の eBGP
  アンダーレイ=Step 10〜11 からの持ち越し解消、EVPN ファブリックの「IGP + ループバック間
  iBGP + スパイン RR」定石とその理由3点)、反射のウォークスルー(区間別属性変化表)、
  confed の AS_PATH 変化図、FRR のスパイン RR 設定例(EVPN ファミリでの
  route-reflector-client)、トラブルシューティング4種(client 指定漏れ・ファミリ別指定、
  クラスタ ID 共有による破棄、パスハイディング、confed の NEXT_HOP 解決不能)を記載。
- 決定事項: (1) 表記: 「ルートリフレクタ」「コンフェデレーション」「メンバー AS」
  (サブ AS を併記)、「反射」(reflect の訳語)、「パスハイディング」を標準表記とする。
  (2) 実務の推奨として「新規設計は RR」「EVPN ファブリックは iBGP + スパイン RR を基本形」
  「冗長 RR のクラスタ ID は既定(個別)のまま」を本書の立場として明示。
  (3) ADD-PATH(RFC 7911)・全 eBGP ファブリック(next-hop-unchanged)は存在の言及のみ。
  (4) multipath relax は RFC 外の実装慣例と明記(管理距離・Weight と同じ扱いのルール)。
  (5) 用語集へ3語登録(ORIGINATOR_ID / CLUSTER_LIST、コンフェデレーション、
  ルートリフレクタ)+ フルメッシュ・経路選択プロセスの関連用語を更新。
  (6) 「後述: 06_large_scale_design」参照5箇所(02章2、03章1、04章1、第2部05章1)を
  すべてリンク化(第2部05章の第3部への未解消参照はこれで完了)。
- 未解決・要検証事項: (1) RFC 4456 Section 9 の経路選択修正の挿入位置は
  「BGP Identifier 比較の直後(ピアアドレス比較の前)」と記載したが、条文の
  Step f)/g) との正確な対応は要再確認。(2) 冗長 RR のクラスタ ID を「個別のままが
  現代の一般的推奨」とした記述はベンダーの設計ガイド類に基づく慣例であり、RFC の
  規定ではない。(3) FRR の `Originator:` / `Cluster list:` の表示・EVPN での
  route-reflector-client の設定体系はバージョンにより微差がありうる。
  (4) RFC 7938 のスパイン層 AS 番号共有・multipath relax の記述は要旨の整理であり、
  条文の細部(ASN 割り当てスキームの選択肢)は簡略化している。
- 次のステップ: Step 18 `04_ipv6/01_why_ipv6.md` から着手する(第4部開始)

## Step 18: `04_ipv6/01_why_ipv6.md` (完了日: 2026-07-07)

- 完了内容: 第4部の起点となる IPv6 導入章を執筆。IPv4 枯渇の構造(クラスフルの粒度・
  経路集約の要請・「グローバル一意性という資源」)、3つの延命策(CIDR / RFC 1918+NAT /
  回収と移転)、IANA 2011年2月枯渇と RIR の順次枯渇、NAPT の状態テーブルと壊した4つのもの
  (無状態性=エンドツーエンド原則、着信、プロトコル介入、「内側は安全」の誤解)、
  CGN(RFC 6888/6598)のポートの算数と限界、IPng 選定(RFC 1752)と「互換性を捨てる決断」
  →デュアルスタック並走の構造的理由、128 ビットの質的意味(/64・階層集約・一意性回復)、
  IPv6 基本ヘッダ(固定40オクテット、IPv4 との対比表 12+可変→8 フィールド)、
  チェックサム廃止と UDP チェックサム必須化、経路上フラグメンテーション廃止
  (Packet Too Big / 最小 MTU 1280 / PMTUD 事実上必須)、拡張ヘッダ(連鎖・主要6種の表・
  RFC 7872 の現実)、ブロードキャスト廃止(NDP への伏線)、Flow Label 言及、
  Linux での「IPv6 は既に動いている」確認例、トラブルシューティング4種(意図しない IPv6・
  ICMPv6 過剰フィルタ・デュアルスタックの遅延と Happy Eyeballs・MTU 1280 未満)を記載。
- 決定事項: (1) 「IPv6 は IPv4 と互換のない別プロトコル」「移行は切り替えではなく並走」を
  第4部全体の基本構図として導入。(2) 「ICMPv6 は補助ではなく IPv6 の可動部品」という
  位置づけを導入(03章・05章から参照する想定)。(3) 章の分担: アドレス表記・/64・
  リンクローカルと %iface・スコープは 02章、NDP/RA/SLAAC は 03章、移行技術は 05章に委ねる。
  Flow Label は言及のみで以後深入りしない。STUN/TURN・NAT66・ALG の詳細は本書の範囲外と
  本文で明示。(4) Routing ヘッダ Type 4 = SRH を第5部(SRv6)への伏線として明示。
  (5) 用語集へ7語登録(CGN、IPv6、NAT、エンドツーエンド原則、拡張ヘッダ、
  デュアルスタック、プライベートアドレス)。(6) 第1部01章の「第4部で扱う」参照
  (Header Checksum 廃止)を本章へのリンクに更新。
- 未解決・要検証事項: (1) RIR 枯渇の時系列は代表例のみ記載(APNIC 2011年4月、
  RIPE NCC 2019年11月完全枯渇)。ARIN 等の日付は挙げていない。細部の日付は要再確認。
  (2) 「世界の利用者の半分近くが IPv6 到達可能(2020年代半ば)」は Google IPv6 統計等の
  概数であり、公開時点での再確認が望ましい。(3) 移転市場「1アドレス数十ドル規模」は
  変動する時価の概数。(4) RFC 7872(拡張ヘッダ廃棄の観測)は 2016 年の測定であり、
  現状はより改善/悪化している可能性がある。(5) 「ROAD 問題」の名称・時期
  (1990年代初頭、RFC 1380 参照)は要旨としての記載。(6) 「バージョン5は ST-II が
  使用済み」は通説として記載(一次情報は IANA の Version Numbers レジストリ)。
- 次のステップ: Step 19 `04_ipv6/02_addressing.md` から着手する

## Step 19: `04_ipv6/02_addressing.md` (完了日: 2026-07-07)

- 完了内容: IPv6 アドレッシングの章を執筆。表記法(RFC 4291 の省略規則と RFC 5952 正規形、
  [addr]:port)、「1インタフェース複数アドレスが正常」という発想の転換を導入の軸に、
  アドレス空間の全体地図(表)、GUA の3層構造と階層割り当て(/32・/48〜/56 = RFC 6177・
  ニブル境界)、/64 の根拠(RFC 4291/2464/7421)と /127 の例外(RFC 6164/6583)、
  スコープ(RFC 4007)とリンクローカルの3性質(必ずある・プロトコルの土台・ゾーン ID)、
  ULA(RFC 4193、乱数グローバル ID の意図、NAT ではなく GUA 併存)、マルチキャスト構造
  (flags/scope = RFC 7346、well-known 表)、エニーキャスト(専用フォーマットなし)、
  アドレス選択(RFC 6724、送信元規則の要約+宛先優先度表、ULA < IPv4)、IID 生成3方式
  (Modified EUI-64 の U/L 反転と RFC 7136、一時アドレス RFC 8981、安定不透明 RFC 7217 +
  RFC 8064)、アドレスの一生(tentative/preferred/deprecated と valid_lft/preferred_lft)、
  要請ノードマルチキャストと 33:33 MAC マッピング(RFC 2464)、Linux での複数アドレス・
  fe80 ゲートウェイ・maddress の読み方、トラブルシューティング4種(fe80 ゲートウェイは
  正常/ゾーン ID 漏れ、一時アドレスと ACL、ULA だけ配ると IPv6 が使われない、表記ゆれ)。
- 決定事項: (1) 前章の EUI-64 例と整合: MAC 52:54:00:12:34:56 → fe80::5054:ff:fe12:3456 を
  本章の変換例に採用し、01章の設定例の伏線を回収。(2) 章の分担: DAD・RA・SLAAC の
  プロトコル動作、アドレスの一生の遷移プロセスは 03章に委ねる(本章は状態と寿命の定義まで)。
  NAT64 WKP(64:ff9b::/96)は地図での言及のみで 05章へ。MLD スヌーピングは一言の言及のみ。
  (3) 「fe80 のデフォルトゲートウェイは正常」「ULA が IPv4 より優先されないのは仕様」を
  本章の運用上の要点として明示。(4) 用語集へ7語登録(GUA、ULA、インタフェース ID、
  エニーキャスト、ゾーン ID、要請ノードマルチキャスト、リンクローカルアドレス)+
  IPv6 の関連用語を更新。(5) 01章の「(後述: `02_addressing.md`)」参照をリンク化。
- 未解決・要検証事項: (1) 一時アドレスの既定寿命「1日程度で世代交代」は RFC 8981 の
  TEMP_PREFERRED_LIFETIME(1日)に基づく概括(旧 RFC 4941 とは既定値が異なり、
  OS により差がある)。(2) RFC 6724 の送信元選択規則の番号付け(規則3・規則4等)は
  本文では要約であり条文の Rule 番号と厳密には対応させていない。(3) Linux の
  `ip -6 addr` の `mngtmpaddr`/`temporary` フラグ表示・`ping fe80::1` の
  エラー文言はカーネル/iputils のバージョンにより微差がありうる。
  (4) 3fff::/20(RFC 9637、2024年追加のドキュメント用)は地図の表に含めたが、
  実務での認知度はまだ低い。
- 次のステップ: Step 20 `04_ipv6/03_ndp_slaac.md` から着手する

## Step 20: `04_ipv6/03_ndp_slaac.md` (完了日: 2026-07-07)

- 完了内容: NDP(RFC 4861)と SLAAC(RFC 4862)の章を執筆。「ARP の置き換えではなく
  境界業務の再設計」を導入の軸に、5メッセージ(RS/RA/NS/NA/Redirect)の一覧、
  ICMPv6 上に作った理由と Hop Limit 255 検査(第3部02章の GTSM と同原理という接続)、
  RA の「放送」モデル vs DHCP の「配給」モデル(ネットワーク側の知識+ホスト側の IID
  という分業 = ステートレスの意味)、アドレスとオンリンクの分離(RFC 5942、L/A フラグ
  独立)、NUD の状態機械(REACHABLE/STALE/DELAY/PROBE、上位層の確認の相乗り)、
  M/O フラグと DHCPv6(RFC 8415、デフォルト経路は配れない、DHCPv6-PD は言及)、
  RA/NA/PIO のフォーマット図とオプション TLV 表(RDNSS = RFC 8106)、ブート時の
  全シーケンス(Mermaid、MLD 参加→DAD→RS/RA→GUA 生成→デフォルト経路)、DAD の詳細
  (送信元 ::、楽観的 DAD RFC 4429)、アドレス解決と NUD の一生のウォークスルー、
  リダイレクト、FRR の RA 設定と tcpdump `ip6[40] == 134` での RA の読み方・
  `ip -6 neigh` の状態の読み方、トラブルシューティング4種(forwarding と accept_ra、
  不正 RA と RA Guard = RFC 6104/6105/6980/7113、DAD 失敗 dadfailed とヘアピン
  = RFC 7527、MLD スヌーピング起因の NS 喪失 = 第2部04章の IGMP クエリア問題の再演)。
- 決定事項: (1) 表記: 「近隣要請(NS)/近隣広告(NA)/ルータ要請(RS)/
  ルータ広告(RA)」を正式訳とし本文では略語を主に使う。「近隣キャッシュ」
  (ARP テーブル相当)、「オンリンク」を標準表記とする。(2) 前章までの伏線を回収:
  要請ノードマルチキャスト(02章)を NS/DAD の土台として、「アドレスの一生」(02章)の
  寿命の供給源を PIO として、「fe80 ゲートウェイ正常」(02章)を RA 送信元として、
  「ICMPv6 は可動部品」(01章)を NDP の実体として明示。02章の `expires 1795sec` を
  Router Lifetime(RA 間隔×3 = 1800秒)の実例として接続。(3) 章の分担: DHCPv6 は
  M/O フラグとの関係(比較)まで(専用章は設けない)。DHCPv6-PD は 05章で再登場と
  明示。SEND(RFC 3971)は言及のみ(普及していないと明記)。ルータ側の IPv6
  (OSPFv3 等)は「(後述: `04_routing_ipv6.md`)」参照。(4) Android が DHCPv6 を
  実装しない事実を「M=1 のみの設計の注意点」として本文に記載(実装の現実の例)。
  (5) 用語集へ8語登録(DAD、DHCPv6、NDP、NS / NA、NUD、RS / RA、SLAAC、オンリンク)+
  IPv6・要請ノードマルチキャスト・リンクローカルアドレスの関連用語を更新。
  (6) 01章の「(後述: 03_ndp_slaac.md)」3箇所・02章の同4箇所をすべてリンク化。
- 未解決・要検証事項: (1) Linux の accept_ra のセマンティクス(1 = ホスト時のみ、
  2 = forwarding 中も受理)はカーネルの挙動として記載(ディストリビューション・
  NetworkManager 側の上書きがありうる)。(2) tcpdump の RA 出力例・`ip -6 neigh` の
  FAILED 表示はバージョンにより文言に微差がありうる。(3) 「多くのスイッチ実装は
  ff02::1 宛て(全ノード)は無条件にフラッディングする」(トラブル④)は代表的挙動と
  して記載したが、実装差がある(MLD スヌーピングの all-nodes 扱いは要検証)。
  (4) NUD の既定値(BaseReachableTime 30秒、DELAY 5秒、ユニキャスト NS 3回、
  RetransTimer 1秒)は RFC 4861 Section 10 の規定値。OS の実際の既定は sysctl で
  変更されている場合がある。(5) RFC 4191 の Prf ビット・RFC 8319(RA 間隔の上限拡張)
  の細部には立ち入っていない。
- 次のステップ: Step 21 `04_ipv6/04_routing_ipv6.md` から着手する

## Step 21: `04_ipv6/04_routing_ipv6.md` (完了日: 2026-07-07)

- 完了内容: IPv6 ルーティングの章を執筆。「原理(ロンゲストマッチ・ホップバイホップ・
  3段フィルタ)は不変、変わるのは3点(リンクローカルネクストホップ/経路上
  フラグメンテーション廃止に伴う ICMPv6 の転送への組み込み/プロトコル自身の対応)」を
  導入の軸に、RA(ホストへの供給)とルーティングプロトコル(ルータ間の交換)の分業、
  リンクローカルネクストホップの3つの理由と RFC 7404(リンクローカルのみのインフラ)、
  静的経路のインタフェース必須、直結経路とオンリンクの分離(L フラグを落とす設計)、
  IGP 対応の2流儀(新版新造 = OSPFv3/RIPng vs 入れ物拡張 = IS-IS TLV/MP-BGP)、
  OSPFv3(RFC 5340)の5変更点(リンクローカル足回り、ルータ ID 識別、per-link +
  Instance ID、トポロジとアドレスの分離 = Link-LSA/Intra-Area-Prefix-LSA、認証の外出し
  = RFC 7166)と RFC 5838、IS-IS(RFC 5308)とマルチトポロジ(RFC 5120)、
  RFC 2545 のネクストホップ2個併記の伏線回収(直結の隣人用と遠くの iBGP ピア用)、
  デュアルスタック = 独立した2枚の地図(ships in the night)、OSPFv3 ヘッダ・
  LS Type(U/S2/S1 ビット)の図と v2 対応表、プレフィックス追加が SPF を走らせない
  ウォークスルー(Mermaid)、Packet Too Big / Time Exceeded とレート制限(RFC 4443/
  4890/8201)、FRR の ospf6 設定例(ネットワーク文にプレフィックスが出ない、
  fe80 ネクストホップの読み方)、トラブルシューティング4種(静的経路のゾーンの罠、
  OSPFv3 隣接 = ルータ ID 欠落・Instance ID 不一致・ff02::5 と MLD スヌーピング再演、
  PMTUD ブラックホール、2枚の地図の不一致 = ファミリ別 traceroute での切り分け)を記載。
- 決定事項: (1) LSA の U ビット(未知の型への動作の内蔵)を BGP の optional/transitive
  フラグと同じ「後方互換な拡張性」の思想として第3部03章に接続した。
  (2) 実務の前提: OSPFv2 + OSPFv3 の並走を本書の以後の前提とする(RFC 5838 での
  一本化は言及のみ)。RIPng は存在の言及のみ。(3) OSPFv3 の LSA 詳細は Function Code
  1〜9 の表まで(NSSA・仮想リンクの細部には立ち入らない)。(4) IS-IS の TLV 拡張性を
  第5部(SR 対応)への伏線として明示。(5) 用語集へ3語登録(OSPFv3、PMTUD、
  マルチトポロジ)+ OSPF(関連用語に OSPFv3)・IS-IS(RFC 5308 と対比の追記)・
  MTU(関連用語に PMTUD)を更新。(6) 03章の「(後述: `04_routing_ipv6.md`)」参照を
  リンク化。
- 未解決・要検証事項: (1) FRR の OSPFv3 設定体系はバージョン差が大きい
  (interface 配下の `ipv6 ospf6 area` は比較的新しい形。旧版は `router ospf6` 配下の
  `interface eth0 area 0.0.0.0`)。出力例(`show ipv6 ospf6 neighbor` の列、
  `ip -6 route` の `proto ospf6` 表記)も代表的な形で記載。(2) 静的経路のエラー文言
  (`IPv6 next hop is link-local...`)は iproute2 の実際の文言の要旨であり要確認。
  (3) 「OSPFv2 ではプレフィックス変更が SPF 再計算を要した」は概括であり、実装は
  スロットリング・増分計算で緩和している(原理の対比としては正しい)。(4) Flow Label を
  ECMP ハッシュに使う実装(RFC 6438 系)は一言の言及のみで RFC 番号は挙げていない。
  (5) OSPFv3 の Hello に prefix が載らない代わりのネクストホップ学習の記述
  (「Link-LSA / Hello の送信元から知る」)は要旨の簡略化(正確にはネクストホップは
  Hello 等の送信元リンクローカルから得る)。
- 次のステップ: Step 22 `04_ipv6/05_transition_technologies.md` から着手する

## Step 22: `04_ipv6/05_transition_technologies.md` (完了日: 2026-07-07)

- 完了内容: 移行技術の章を執筆(第4部完結)。「デュアルスタックは IPv4 アドレスを消費し
  続けるという自己矛盾」→「IPv4 as a Service(RFC 9313)への転回」を導入の軸に、
  道具の2分類(カプセル化=中身を無傷で運ぶ vs 変換= RFC 7915 で書き直す)と
  設計軸「状態の置き場所」(CGN の反省 → A+P = RFC 6346)で全方式を整理。
  黎明期の IPv6-over-IPv4(6in4/6to4 = RFC 7526 で非推奨/Teredo/6rd — 「アドレスに
  埋め込めば状態を消せる」の系譜)、DS-Lite(RFC 6333、B4/AFTR、B4 アドレスを連結キーに
  含む拡張 NAPT テーブル、192.0.0.0/29、RFC 6334)、MAP-E(RFC 7597、BMR/EA-bits/PSID、
  ポートオフセット a=6 の算数 = 共有比 256 で 252 ポートの具体例、BR ステートレス=
  エニーキャスト冗長・メッシュモード、MAP-T/lw4o6 は位置づけのみ)、NAT64/DNS64
  (RFC 6146/6147、64:ff9b::/96 = 02章の伏線回収、射影の3つの穴=リテラル・独自リゾルバ・
  DNSSEC)、464XLAT(RFC 6877、CLAT/PLAT、RFC 7050/8781 のプレフィックス発見、
  RFC 8925 option 108)、方式比較表、ウォークスルー3本(DS-Lite の Mermaid、MAP-E の
  アドレス計算、NAT64/DNS64 の Mermaid)、MTU の算数(二層 PMTUD、MSS クランプ 1420)、
  設定例(dig での DNS64 合成観察 = ipv4only.arpa、Linux ip6tnl での B4 手組み)、
  トラブルシューティング4種(MAP-E ポート枯渇=CGN の算数の再演と可観測性の違い、
  トンネル MTU の二層構造、NAT64 で特定アプリだけ死ぬ、ポート開放不可は方式の仕様)を記載。
- 決定事項: (1) 表記: 「IPv4 as a Service(IPv4aaS)」「カプセル化/変換」「状態の
  置き場所」を本章の整理軸として導入。「射影」を DNS64 の動作の説明語として使用。
  (2) 章の分担: 本章は IPv4↔IPv6 共存技術のみ。トンネル一般論の続き(MPLS/SRv6 も
  カプセル化の同族)は第5部へ接続。NAT の挙動分類(EIM 等)・PCP(RFC 6887)は言及のみ。
  6rd/6to4 等の IPv6-over-IPv4 は歴史として1節に圧縮。(3) 特定 ISP のサービス名は
  挙げず、RFC の一般論として記述(CLAUDE.md の方針どおり)。(4) 03章の DHCPv6-PD、
  02章の 64:ff9b::/96、01章の CGN/エンドツーエンド原則の伏線をすべて回収。
  (5) 用語集へ6語登録(464XLAT、A+P、DNS64、DS-Lite、MAP-E、NAT64)+ CGN・NAT・
  デュアルスタックの関連用語を更新。(6) 01章・02章・03章の「(後述:
  05_transition_technologies.md)」参照3箇所をすべてリンク化(第4部内の未解消参照はゼロに)。
- 未解決・要検証事項: (1) MAP-E のポート例(PSID=52 → 1232〜1235 等)は RFC 7597
  Appendix B の既定アルゴリズム(a=6)による計算であり、実際の ISP のルールとは
  異なりうる。(2) 「Android/iOS は CLAT を標準実装」は概括(実装範囲・有効化条件は
  OS バージョン・回線種別による)。(3) `dig @dns64.dns.google` の出力例は公開サービスの
  現時点の挙動(サービス継続性は保証されない)。(4) RFC 6333 の「アクセス網 MTU を
  1540 以上に」は推奨の要旨(条文は ≥1540 を SHOULD として勧める形、要再確認)。
  (5) ip6tnl の mode ipip6 での IPv4-in-IPv6 は Linux の代表的な手段として記載
  (カーネルバージョンによりオプションに差がありうる)。
- 次のステップ: Step 23 `05_mpls_srv6/01_mpls_basics.md` から着手する(第5部開始)

## Step 23: `05_mpls_srv6/01_mpls_basics.md` (完了日: 2026-07-07)

- 完了内容: 第5部の起点となる MPLS 基礎の章を執筆(RFC 3031/3032)。「高速化という当初の
  動機は消滅した。残った価値は転送の根拠を宛先 IP から切り離せること」を導入の軸に、
  3つの応用(BGP フリーコア/VPN/TE)の予告、カプセル化の同族としての位置づけ
  (第4部05章の2分類に接続、ただし 4 オクテットの最小ヘッダ)、FEC(分類の一回性と自由度)、
  ラベル=リンクごとのローカルな合意(VID/VNI との対比、L2 アドレスの毎ホップ書き換えとの
  相似)、LSR/LER と P/PE/CE、push/swap/pop、LFIB(FTN/ILM/NHLFE)、ラベルスタック
  (トンネル+サービスの分業、VXLAN の外側 IP + VNI と同型)、PHP(implicit/explicit NULL、
  予約ラベル 0〜15、RFC 7274)、コントロールプレーンの分離(配布方法の一覧表 = 次章以降の
  地図)、ラベルフォーマット図(TC = 旧 EXP、RFC 5462)、TTL の uniform/pipe(RFC 3443)、
  BGP フリーコアのウォークスルー(第3部02章のトランジット AS 構成を MPLS 込みで完走、
  区間別の表)、MTU 設計、Linux での静的 LFIB 設定例、トラブルシューティング4種
  (コア MTU と RFC 3032 の ICMP トンネリング、traceroute の見え方 = TTL モデル、
  サイレントなラベル破棄と MPLS ping = RFC 8029、PHP による TC 消失)を記載。
- 決定事項: (1) 表記: 「ラベルスイッチング」「LSP」「LSR / LER」「P / PE / CE」
  「PHP」「ラベルスタック」を標準表記とする。「レイヤ 2.5」は俗称として言及のみ。
  (2) 章の分担: 本章はデータプレーン(swap の仕組み)まで。ラベル配布(LDP/RSVP-TE)は
  02章、VPN(2段ラベルの実用)は 03章、SR は 04章に委ねる(本文の表で明示)。
  (3) 第3部02章の BGP フリーコア(解決策②)の伏線を本章のウォークスルーで正式に回収し、
  同章の「後述」参照をリンク化。(4) P/PE/CE の用語は RFC 4364 由来と明記した上で
  本章から使用開始(正式な文脈 = L3VPN は 03章)。(5) 用語集へ7語登録(FEC、LFIB、LSP、
  LSR / LER、MPLS、PHP、ラベルスタック)+ カプセル化の関連用語に MPLS を追加。
- 未解決・要検証事項: (1) RFC 3032 の ICMP トンネリング(エラーを LSP の先まで運んでから
  返す)は仕様の趣旨の要約であり、条文の正確な規定(Section 2.3.2)の再確認が望ましい。
  (2) Linux の MPLS 設定例(`ip -f mpls route`、`encap mpls`)はカーネル 4.x 以降の
  代表的な形(ディストリビューションにより mpls_router モジュールのロードが必要)。
  (3) 予約ラベル 7(ELI)・13(GAL)は言及のみで深入りしていない(Entropy Label は
  RFC 6790。必要なら 02章の ECMP の文脈で補う)。(4) 「MPLS にはフラグメンテーション
  機構がない」は正確には「ラベル層には無い(中身の IP 層に委ねる)」の意。
- 次のステップ: Step 24 `05_mpls_srv6/02_ldp_rsvp_te.md` から着手する

## Step 24: `05_mpls_srv6/02_ldp_rsvp_te.md` (完了日: 2026-07-08)

- 完了内容: LDP と RSVP-TE の章を執筆。「問い A: 基本の道に自動でラベルを(LDP)/
  問い B: IGP が選ばない道を意図的に(RSVP-TE)」の2部構成。LDP(RFC 5036)は
  「経路を決めないプロトコル = IGP への従属」を軸に、発見(UDP 646)とセッション
  (TCP 646、transport address)、配布3軸(DU/liberal/independent が定番 =
  「メモリを使って収束を速く」、第1部04章の伝搬と計算の分離に接続)、LDP-IGP 同期
  (RFC 5443、復旧時にだけ壊れる障害)。RSVP-TE(RFC 3209)は fish problem による
  TE の動機づけ、部品3点(IGP TE 拡張 RFC 3630/5305 = 資源つき地図、CSPF =
  入口の意思・ソースルーティングの復権、PATH/RESV = ERO と下流からのラベル)、
  ソフトステート(RFC 2961 言及)と N² の状態問題(第3部06章のフルメッシュの再演)、
  帯域予約 = 帳簿(自己申告)の指摘、「張る」と「載せる」の区別(autoroute)、
  FRR(RFC 4090、PLR のローカル修復、FRRouting との略称衝突を明記)、比較表と
  SR への動機(状態をパケットへ)、LDP メッセージ表・セッション確立と配布の
  Mermaid 図(前章の天下りラベルの出所を完成)、RSVP-TE シグナリングの Mermaid 図、
  strict/loose ERO、FRRouting ldpd 設定例(`show mpls ldp binding` の Local/Remote/
  In Use の読み方 = liberal retention の可視化)、トラブルシューティング4種
  (LDP-IGP 同期欠如、transport address 不到達 = update-source 漏れと同型、
  CSPF no path = TE 地図の欠け・帳簿満杯、LSP up なのに乗らない = FIB の結線)を記載。
- 決定事項: (1) 表記: 「LDP-IGP 同期」「CSPF」「ERO」「ヘッドエンド/テール」
  「ソフトステート」を標準表記とする。高速迂回は FRRouting と衝突するため
  「FRR(RFC 4090)」と RFC 番号併記で書き分ける(glossary にも明記)。
  (2) 章の分担: LDP の細部(セッション保護、graceful restart)、RSVP-TE の
  オブジェクト形式の網羅には立ち入らない。FRR(RFC 4090)は思想(事前計算+
  ローカル修復)までとし、TI-LFA は 04章に委ねる。DiffServ-TE・帯域の
  優先度/プリエンプションは扱わない。(3) 「状態の置き場所」(第4部05章の設計軸)を
  第5部の背骨として再使用することを明示(SR への伏線)。(4) mLDP・RSVP の
  IntServ 時代の経緯は一言のみ。(5) 用語集へ6語登録(CSPF、Fast Reroute、LDP、
  RSVP-TE、ソフトステート、トラフィックエンジニアリング)。(6) 01章の
  「(後述: 02_ldp_rsvp_te.md)」参照をリンク化。
- 未解決・要検証事項: (1) LDP セッションの能動側の決定(transport address の
  大小比較)は RFC 5036 Section 2.5.2 の要旨であり、条文の正確な表現は要再確認。
  (2) RSVP のリフレッシュ既定 30 秒は RFC 2205 の R = 30s に基づく(実装の既定は
  異なりうる)。(3) FRRouting の ldpd 設定・show 出力はバージョンにより微差がありうる
  (`discovery transport-address` の体系は代表的な形)。(4) OSPF の LDP 同期
  コマンド名(`mpls ldp-sync`)は FRR の実装に基づく。(5) 「RSVP-TE の帯域に
  データプレーンの絞りはない」は一般論(実装によっては auto-bandwidth や
  ポリサ連動があるが本文では触れていない)。
- 次のステップ: Step 25 `05_mpls_srv6/03_l3vpn_l2vpn.md` から着手する

## Step 25: `05_mpls_srv6/03_l3vpn_l2vpn.md` (完了日: 2026-07-08)

- 完了内容: MPLS VPN の章を執筆。「専用線の置き換え=1つの共用網に互いに見えない
  多数の網を」という商売の要求3点(アドレス重複許容/経路分離/コア無関知)から導入し、
  L3VPN(RFC 4364)を3部品 — VRF(テーブル分離、IVL→MAC-VRF と同系譜)、
  MP-BGP VPNv4(RD = 一意化・RT = 仕分け、第2部05章の RD/RT の「出生地」種明かし、
  RT によるハブ&スポーク等のトポロジ制御)、2段ラベル(トンネル+ VPN、
  対称 IRB の L3VNI は「VPN ラベルの VXLAN 方言」)— として整理。
  L3VPN vs EVPN/VXLAN の対応表(第2部との橋)、PE-CE(as-override/allowas-in は
  ノブとして言及)、L2VPN の系譜(疑似回線 = RFC 3985/8077・VPWS →
  VPLS = RFC 4761/4762・PW フルメッシュ+スプリットホライズン = iBGP と同型 →
  限界3点 → EVPN は本来 MPLS の技術で RFC 8365 が後、という逆輸入の種明かし)、
  6PE/6VPE(RFC 4798/4659、IPv4aaS の鏡像)、VPNv4 NLRI の構造図(ラベルは NLRI・
  RT は属性という置き場所の意味)、経路とパケットのフルウォークスルー
  (Mermaid + 区間別ラベルスタック表、PHP の実益)、RT Constraint(RFC 4684)、
  FRR の VRF + vpnv4 設定例、トラブルシューティング4種(RT 不一致 = 層構造の再利用、
  LSP 断で VPN だけ全滅 = IP は健全という非対称、同一 RD とパスハイディング、
  ラベル2枚の MTU と PW の MTU 不一致検査)を記載。
- 決定事項: (1) 表記: 「L3VPN」「VPNv4(VPN-IPv4)」「疑似回線(pseudowire、PW)」
  「VPLS」「VRF」を標準表記とする。(2) 本章の VPN は経路分離型(事業者 VPN)であり
  暗号化 VPN(IPsec)は対象外と冒頭で明示。(3) 章の分担: PE-CE ルーティングの詳細
  (OSPF スーパーバックボーン、SoO 等)、CsC(Carrier's Carrier)、inter-AS
  オプション A/B/C は扱わない。マルチキャスト VPN(RFC 6513)も対象外。
  (4) 「知識を縁に寄せ中間を単純に」をエンドツーエンド原則の網内版として位置づけ、
  第5部の縦糸(状態の置き場所)に接続。(5) EVPN の歴史的順序(VPLS の反省 →
  EVPN-MPLS → EVPN-VXLAN)を正式に記述し、第2部が「方言から先に学んだ」ことを
  明示。(6) 用語集へ5語登録(L3VPN、VPLS、VPN-IPv4、VRF、疑似回線)+
  RD(マルチホームの RD 分離の技法を追記・初出章欄更新)・RT(初出章欄更新)。
  (7) 第2部05章・第3部05章の「第5部で扱う」参照4箇所と第5部01章の
  「(後述: 03_l3vpn_l2vpn.md)」2箇所をリンク化。
- 未解決・要検証事項: (1) VPN ラベルの割当粒度(per-VRF / per-prefix / per-CE)は
  実装差の話題として本文では per-VRF 相当の挙動を暗黙の前提にした(明示的な比較は
  していない)。(2) FRR の L3VPN 設定(`label vpn export auto`、`rd vpn export` 等)は
  比較的新しい体系でバージョン差がありうる。(3) 「多くの実装はネクストホップが
  ラベル付き経路で解決できない VPN 経路を無効扱いにする」は代表的挙動
  (nexthop validation の細部は実装差が大きい)。(4) RFC 4684(RT Constraint)の
  SAFI 値 132 は要再確認(本文には数値を記載)。(5) VPWS の EVPN 版(EVPN-VPWS、
  RFC 8214)には触れていない。
- 次のステップ: Step 26 `05_mpls_srv6/04_srv6.md` から着手する(最終ステップ)

## Step 26: `05_mpls_srv6/04_srv6.md` (完了日: 2026-07-08)

- 完了内容: セグメントルーティングと SRv6 の章を執筆(第5部・全ステップ完結)。
  02章の宿題「TE の自由を中間状態なしで」→「経路の知識をパケットへ移す」
  (RFC 8402、状態の置き場所の縦糸の最終回答、CGN→MAP-E と同型の転回)を導入の軸に、
  セグメント=命令(Prefix-SID / Adjacency-SID、逸脱点だけ指定 = loose の徹底)、
  IGP 拡張による SID 配布(RFC 8665/8667、IS-IS TLV の伏線回収)→ LDP 廃止・
  RSVP-TE 不要、SR-MPLS(RFC 8660、SRGB)、SRv6(RFC 8754、SRH = Routing Type 4 の
  第4部01章伏線回収、「中継はただの IPv6 転送 = 非対応ルータ混在可」)、
  RFC 8986 network programming(Locator + Function、End / End.X / End.DT4/DT6 /
  H.Encaps、「アドレスが命令になる」)、VPN over SRv6(RFC 9252、前章3部品との
  対応表、MPLS なしの BGP フリーコア)、TI-LFA(FRR 思想の状態なし版、RFC 5286 の
  限界解消、マイクロループ対策 RFC 8333 = 第1部04章からの持ち越し解消)、
  代償2点(ヘッダ長 → 圧縮 SID = RFC 9800、SR ドメインの信頼境界 = RH0/RFC 5095 の
  反省)、SRH フォーマット図(逆順格納・Segments Left)、End の擬似コード、
  TE ウォークスルー(区間別 DA/SL 表)、reduced encapsulation、Linux seg6 設定例
  (encap seg6 / seg6local End.DT4)、トラブルシューティング4種(SID への ping は
  機能を証明しない、SRH 廃棄 = RFC 7872 再演とドメイン境界の正常動作、
  可変オーバーヘッドの MTU、traceroute の DA 書き換わり)を記載。
  まとめで本書全体の縦糸(転送とサービスの分離・状態の置き場所・知識を縁へ)を総括。
- 決定事項: (1) 表記: 「セグメントルーティング(SR)」「SID」「SRH」「SRv6」
  「TI-LFA」を標準表記とする。SR-MPLS は SR の用語内で扱い単独エントリは設けない。
  (2) 章の分担・範囲: SR ポリシー(RFC 9256)・PCE・BGP-LS によるコントローラ連携、
  Flex-Algo、SRv6 の OAM 詳細は本書の範囲外(存在の言及もごく短く)。
  圧縮 SID は RFC 9800 の言及まで(方式の詳細には立ち入らない)。
  (3) 第1部04章(マイクロループ = RFC 8333)・第4部01章(SRH)・第4部04章
  (IS-IS TLV と SR)の持ち越し・伏線をすべて解消し、該当章の参照をリンク化。
  (4) 用語集へ5語登録(SID、SRH、SRv6、セグメントルーティング、TI-LFA)+
  MPLS の関連用語を更新。(5) 05_mpls_srv6 内の「後述: 04_srv6.md」参照
  (01章・02章の計4箇所)をリンク化。これで全26ステップの未解消「後述」参照はゼロ。
- 未解決・要検証事項: (1) 圧縮 SID の RFC 番号(RFC 9800)と「uSID を含む」という
  記述は要再確認(C-SID の標準化は 2024〜2025 年の完了で、番号・包含関係の細部に
  自信が持てない)。(2) RFC 9252(BGP Overlay Services for SRv6)の SID の運ばれ方
  (Prefix-SID 属性の SRv6 L3 Service TLV)は本文では「BGP が SID を付けて配る」と
  要旨のみ記載。(3) Linux の seg6 設定例(`encap seg6 mode encap` /
  `seg6local action End.DT4 vrftable`)はカーネル 4.14 以降の代表形
  (End.DT4 は 5.11 以降。vrftable の体系はバージョン差あり)。
  (4) 「SID への ping に PE が応答する」挙動は実装による(応答しない実装もある)。
  (5) RFC 8333 は正確には uloop 回避の一方式(delay)であり、セグメントリストによる
  明示経路継続と並ぶ選択肢として簡略化して書いた。条文との対応は要再確認。
- 次のステップ: なし(全ステップ完了)。以後は推敲・要検証事項の解消を行う

## Step 27: `06_redundancy/01_stp_basics.md` (完了日: 2026-07-08)

- 完了内容: 第6部(冗長化)の起点となる L2 ループと STP の章を執筆。「冗長化の
  2本目のケーブルが網を殺す」から導入し、L2 ループが致命的な3因(TTL 不在=透過性
  ゆえに経由回数を記録できない/フラッディングの複製増殖=ブロードキャストストーム/
  MAC 学習の破壊= MAC フラッピング)を第1部01章の透過的ブリッジングに接続。
  全域木という定式化(木の上では道が一意)、STP =単一宛先(ルート)への
  ディスタンスベクタという位置づけ(第1部04章に接続、エージングも同型)、
  3つの選出(ルートブリッジ/ルートポート/指定ポート)と4段比較(BGP の
  経路選択プロセスとの発想の対比)、パスコスト新旧2表(802.1D-1998 / 802.1t)、
  BPDU の運ばれ方(01:80:C2:00:00:00、LLC 0x42、タグなし=第2部02章の伏線回収、
  中継せず再生成)、Configuration BPDU フォーマット図(35 オクテット、1/256 秒単位)、
  タイマー3種の「ルートの独裁」と直径7の仮定(STP は設計時点で L2 の大規模化を
  想定しない)、ポート状態遷移(Blocking〜Forwarding、なぜ 30 秒待つか)、
  収束 30/50 秒の内訳、トポロジ変更(TCN/TCA/TC、MAC エージング短縮が全消去で
  ない理由、Mermaid 図)、三角形トポロジのウォークスルー、Linux bridge 設定例
  (`bridge link show` の state、tcpdump での BPDU の読み方)、トラブル
  シューティング4種(ブロードキャストストーム=アンマネージドスイッチ混入、
  ルート乗っ取り=プライオリティ未設計、片方向リンク= BPDU 不在のフェイル
  オープン性、TCN 氾濫= PortFast/edge port への伏線)を記載。
- 決定事項: (1) **第6部の章構成を確定**: 01_stp_basics(STP)→ 02_rstp_mstp
  (RSTP/MSTP)→ 03_fhrp_vrrp(FHRP: VRRP/HSRP)→ 04_lag_mlag(LACP/MLAG、
  STP に頼らない設計)。README の拡張予定(FHRP + STP)に LAG/MLAG を1章追加した
  (L2 冗長化の現代の主流であり、第2部の EVPN マルチホーミングへの橋になるため)。
  CLAUDE.md に「拡張ステップ」表(Step 27〜30)とディレクトリ構造を追記、
  README に第6部セクション(執筆中マーク)を追加。(2) 表記: 「スパニングツリー
  プロトコル(STP)」「ルートブリッジ」「ルートポート/指定ポート」「ブロッキング」
  「ブロードキャストストーム」「MAC フラッピング」を標準表記とする。802.1D は
  「2014 年改訂で 802.1Q に統合」の既存ルール(style_guide)に従い記載。
  (3) 章の分担: 本章は古典 802.1D のみ。RSTP の高速化機構・edge port・MSTP・
  PVST+ の詳細は 02章に委ねる(PortFast/BPDU ガード/ルートガード/UDLD は
  ベンダー拡張として言及のみ)。(4) Step 8 からの持ち越し(STP を本書でどこまで
  扱うか)を解消: 第2部02章の「STP は本書では主題として扱わない」の記述を
  本章へのリンクに更新。第2部03章のスパニングツリー言及もリンク化。
  (5) 用語集へ6語登録(BPDU、MAC フラッピング、STP、ブロードキャストストーム、
  ルートブリッジ、ルートポート / 指定ポート)。
- 未解決・要検証事項: (1) 「Message Age は各ブリッジで 1 秒相当を加算するのが
  通例」は実装慣例として記載(802.1D の規定は increment の粒度に幅がある。要再確認)。
  (2) TCN 受領後にルートが TC フラグを立て続ける期間(Max Age + Forward Delay =
  35 秒)は 802.1D の規定値として記載したが条文の再確認が望ましい。(3) Linux の
  `bridge link show` の cost 表示は 16 ビット系の値(1G = 4)を例示した(カーネルの
  既定コスト体系はレガシー値。バージョン差あり)。(4) 802.1t のプライオリティ
  4 ビット化+システム ID 拡張の導入版・年は概括(「802.1t での変更」とのみ記載)。
  (5) 第1部・第4部の章での STP への言及は今回2箇所のみリンク化した(他部に
  暗黙の言及が残っていないかは未精査)。(6) 00_overview/01_roadmap.md は
  まだ5部構成のまま。第6部完結時に依存関係グラフ・読了ルートへ第6部を
  組み込む更新を行う(Step 30 完了時の宿題)。
- 次のステップ: Step 28 `06_redundancy/02_rstp_mstp.md` から着手する

## Step 28: `06_redundancy/02_rstp_mstp.md` (完了日: 2026-07-10)

- 完了内容: RSTP/MSTP の章を執筆。802.1D の遅さの正体を「タイマー待ち(時間の経過を
  安全の根拠にする)」と棚卸しし、RSTP を「明示的な確認への置き換え」として整理——
  ①ブロッキングの2分(代替ポート=事前計算済みバックアップの即時昇格、バックアップ
  ポート)と役割/状態の分離(5状態→3状態の対応表)、② Proposal/Agreement + sync
  (「下流の先を塞いでから開ける」握手、Discarding の壁を縁へ押し出す波、
  ポイントツーポイント前提と共有リンクでの退行)、③心拍の分散化(全ブリッジが BPDU
  生成、3×Hello=6秒での破棄= BGP ホールドタイムと同型、劣位 BPDU の即時受理)、
  ④ TC の直接フラッディング+即時フラッシュ(TCN 廃止、Forwarding 遷移のみ発火)。
  UplinkFast/BackboneFast/PortFast →標準機構への対応も記載。MSTP は「VLAN の数だけ
  木を作らず、木の形の数だけ MSTI を作って多対一に写像」(FEC と同型)を軸に、
  PVST+ の O(VLAN数) 問題、MST リージョン(3点セット+ HMAC-MD5 ダイジェスト)、
  CIST/IST(OSPF エリアと同型の階層化、リージョン=仮想ブリッジ、内外コスト分離、
  Remaining Hops)、BPDU 1本への MSTI 相乗り。RST BPDU の Flags 8 ビット図・
  MST BPDU 構造図・P/A の Mermaid 図・前章の三角形での障害切替2ケース
  (代替ポート昇格/劣位 BPDU 受理)、mstpd の設定例(`mstpctl showport` の
  役割/状態の読み方)、トラブルシューティング4種(共有リンク/レガシー混在の退行、
  互換モードの片道性=手動復帰、MST リージョン分裂、TC 嵐と即時フラッシュ)を記載。
- 決定事項: (1) 表記: 「RSTP」「MSTP」「MST リージョン」「代替ポート/バックアップ
  ポート」「エッジポート」「Proposal/Agreement(提案/合意)」を標準表記とする。
  PVST+ は Cisco 由来の固有名として慣用名ルール(仕様に不在の旨併記)で扱った。
  (2) 標準の系譜を明記: 802.1w(2001)→ 802.1D-2004 で本体統合(古典 STP は削除)、
  802.1s(2002)→ 802.1Q-2005 以降統合。「現行 802.1Q のスパニングツリーとは
  RSTP/MSTP のこと」と位置づけ。(3) 本書の縦糸への接続: 代替ポート=事前計算
  バックアップ(フローティングスタティック/TI-LFA と同系)、3×Hello = BGP
  ホールドタイムの作法、MSTI 写像= FEC、CIST/リージョン= OSPF エリアの階層化、
  を明示的に参照。(4) dispute 機構は前章の片方向リンク問題(症状3)への部分的
  標準解として Flags の文脈で記載(深入りせず)。(5) 章の分担: FHRP は次章 03、
  LAG/MLAG は 04 章。まとめで「木である限りの原理的制約」を次章以降への接続として
  明示。(6) 用語集へ5語登録(MSTP、MST リージョン、RSTP、エッジポート、
  代替ポート/バックアップポート)+ STP・BPDU の定義/関連用語を更新
  (BPDU に RST/MST BPDU への統合を追記)。(7) 前章 01 の「次章」参照6箇所を
  02_rstp_mstp.md へのリンクに更新。
- 未解決・要検証事項: (1) RSTP の互換モードからの復帰が手動である点は代表的実装の
  挙動として記載(802.1Q の Protocol Migration ステートマシンには mdelay 経由の
  再検出があり、「手動のみ」は簡略化。mstpctl portmcheck / Cisco の
  clear spanning-tree detected-protocols を例示)。(2) TC を送り続ける期間
  (TC While タイマー)は「数 Hello」とぼかして記載(802.1Q の正確な値は
  HelloTime + 1 秒系で、条文の再確認が望ましい)。(3) MST リージョンのダイジェスト
  計算(HMAC-MD5、規格定義の固定鍵)は鍵の値までは記載していない。
  (4) mstpctl showport の出力例・tcpdump の RSTP 表示(port-role の文言等)は
  代表的な形(バージョンにより微差)。(5) 「リンクタイプは全二重なら
  ポイントツーポイントと推定」は既定動作の代表形(設定で上書き可能な旨は記載済み)。
  (6) MSTI Configuration Message 内のフィールド列挙は要約(条文順ではない)。
- 次のステップ: Step 29 `06_redundancy/03_fhrp_vrrp.md` から着手する

## Step 29: `06_redundancy/03_fhrp_vrrp.md` (完了日: 2026-07-10)

- 完了内容: FHRP(VRRP / HSRP)の章を執筆。「L2 をどれだけ冗長化しても、ホストが知る
  デフォルトゲートウェイは1つ」という穴の解剖(経路テーブル・ARP キャッシュの2段で
  ブラックホール化)から導入し、ホスト側の解の不成立(静的2個・IRDP RFC 1256・
  ホストへの IGP)→「ホストを直さず、仮想 IP + 仮想 MAC という『役』をルータが
  引き継ぐ」転回として FHRP を整理。仮想 MAC の必然を「2冊の台帳」(全ホストの
  ARP キャッシュ/スイッチの FDB)で説明し、修正対象を FDB 1点に縮約する構図を軸にした。
  VRRP(RFC 9568): マスタのみが広告する心拍の経済性(RSTP の全員心拍との対比)、
  3 × 広告間隔 + Skew_Time(タイマー自体が選挙)、Priority 0 = 辞任、オーナー(255)と
  プリエンプション(VRRP 既定有効 / HSRP 既定無効)、TTL 255 検査(GTSM と同原理)、
  v3 での認証削除の設計判断、IPv6 での存在意義(RA/NUD の確率的回復 vs 網側の決定論的
  回復)、ADVERTISEMENT 1種類のパケットフォーマット図(Max Adver Int はセンチ秒)、
  3状態ステートマシン(Mermaid)、急死(≈3.61 秒)vs 計画停止(≈0.61 秒)の
  ウォークスルー、VRRP が守らないもの(アップリンク断=トラッキングは RFC 外の実装慣例、
  戻り経路の非対称とステートフル機器)、HSRP 対比表(RFC 2281、UDP 1985、Hello 3s/Hold 10s、
  メッセージ3種 vs 1種)、VLAN ごとのマスタ交互配置と STP/MSTI ルートとの整合
  (トロンボーン問題)、エニーキャストゲートウェイ(第2部05章)への接続、keepalived の
  設定例(既定が実 MAC + GARP 方式である点を「2冊の台帳」の選択として考察、
  vrrp_version 3 / use_vmac)、トラブルシューティング4種(スプリットブレイン=広告不達と
  MAC フラッピング、切替後の数分断=台帳の直し損ね、アップリンク断=トラッキング未設定と
  監視設計、頻繁な交代=プリエンプト揺り戻し・v2/v3 混在・TTL≠255)を記載。
- 決定事項: (1) 表記: 「FHRP」「VRRP」「HSRP」「仮想ルータ」「マスタ/バックアップ」
  「プリエンプション」を標準表記とする。FHRP は仕様に登場しない業界慣用名である旨を
  慣用名ルールに従い明記。**RFC 9568 が Master → Active に改称した**ことを初出時に
  併記した上で、実装・文献で広く使われる「マスタ/バックアップ」を本書の標準表記に採用
  (glossary の VRRP エントリにも明記)。(2) 章の分担: GLBP は存在の言及のみ。
  ファイアウォール等ステートフル機器の冗長化(状態同期)は範囲外と本文で明示。
  トラッキングは「RFC 外の実装慣例」(管理距離・Weight と同じ扱いのルール)として記載。
  (3) 本書の縦糸への接続: 「名前と実体の分離」(仮想ルータ→エニーキャストゲートウェイ)、
  「IP 転送の無状態性が冗長化を軽くする」(エンドツーエンド原則の再演)、
  3 × keepalive の作法(BGP ホールドタイム・RSTP 3×Hello と同型)、TTL 255 = GTSM、
  監視対象の構造が心拍の設計を決める(VRRP 1台 vs RSTP 全員)を明示的に参照。
  (4) 次章(04_lag_mlag)への接続: 「役は常に1人という前提を外す」方向として
  エニーキャストゲートウェイと MLAG を位置づけ。(5) 用語集へ5語登録(FHRP、HSRP、
  VRRP、仮想ルータ、プリエンプション)+ デフォルトゲートウェイ(単一障害点の記述と
  関連用語)・エニーキャストゲートウェイ・GTSM(VRRP を追記)を更新。
  (6) 前章 02 のまとめの FHRP 言及をリンク化。README の第6部の表で 03 章をリンク化。
- 未解決・要検証事項: (1) RFC 9568 の Master → Active 改称に伴う**タイマー名等の正確な
  改称範囲**(Master_Down_Interval 等の新名称)は条文未確認のため、本文・用語集は旧名
  (RFC 5798 系)で記載した。(2) VRRPv3 の**チェックサムが IPv4 でも擬似ヘッダを含む**と
  した記述(v2 との相違)は要再確認。(3) IPv6 VRRP で「マスタが仮想リンクローカルを
  送信元に RA を送る」とした記述は RFC 9568 の該当箇所の文言要確認(概念としては正だが
  規定の主体が VRRP か運用かは曖昧に書いた)。(4) HSRP の細部(Hello/Coup/Resign の
  3メッセージ、HSRPv2 の差分)は RFC 2281 と Cisco 文書の記憶に基づく代表的記述。
  (5) keepalived の既定動作(VRRPv2、実 MAC + GARP、use_vmac で macvlan)・tcpdump の
  VRRPv3 表示(`intvl 100cs` 等)はバージョンにより差がありうる。(6) IRDP(RFC 1256)の
  「広告間隔が分オーダー」は既定値の要旨(正確な既定: 7〜10 分、lifetime 30 分)。
  (7) 00_overview/01_roadmap.md の第6部組み込みは Step 30 完了時に行う(Step 27 からの
  持ち越し)。
- 次のステップ: Step 30 `06_redundancy/04_lag_mlag.md` から着手する(第6部最終章)

## Step 30: `06_redundancy/04_lag_mlag.md` (完了日: 2026-07-10)

- 完了内容: LAG/MLAG の章を執筆(第6部完結)。「2本と数えるからループになる —
  1本の論理リンクに見せればループは定義から消える」を軸に、リンクアグリゲーション
  (IEEE 802.1AX、802.3ad からの移管の系譜)が買う3つのもの(帯域加算/木の再計算なしの
  高速切替/STP からの独立)、フロー単位ハッシュの必然(802.1AX の要求は「同一会話内の
  順序保証と非複製」のみ、ECMP と同原理・同限界)、静的 LAG の危うさ(合意なき束=
  ループ・誤配送、STP は救えない)→ LACP による明示的合意(システム ID・キーの全メンバー
  一致、Partner 欄の双方向確認、Active/Passive)、心拍(fast 1s/3s・slow 30s/90s の
  3×作法)と**フェイルクローズ**(BPDU 不在=開く vs LACPDU 不在=束から外す、の対比)、
  LAG の1対1制約→ MLAG(共通システム ID を2台で演じる、標準不在= vPC/MC-LAG 等、
  ICCP RFC 7275・DRNI 802.1AX-2014 の言及)、ピアリンクと転送規則(ローカル優先+
  ピアリンク越しをメンバーへ出さない=本書3度目のスプリットホライズン)、
  スプリットブレイン(別経路キープアライブ+セカンダリ抑止)、「役の数え方」対比表
  (STP/FHRP/MLAG/エニーキャスト GW+EVPN)による第6部総括、ESI-LAG(第2部05章)への
  接続、「STP に頼らない設計」の全体像(STP は保険に格下げ)。LACPDU の TLV 構造図
  (Slow Protocols 0x8809、01:80:C2:00:00:02、110 オクテット)、State 8 ビットの表と
  Sync→Collecting→Distributing の順序、MUX 状態機械(Mermaid)、Marker プロトコル、
  障害切替ウォークスルー2ケース(物理断=即時 vs 相手死亡=LACP タイムアウト)、
  MLAG の同期内容とシャーシ喪失の処理、Linux bonding 設定例(port state 63 の読み方、
  tcpdump での LACPDU)、トラブルシューティング4種(ハッシュ偏り= layer2 既定と
  ルータ越しトラフィック、束が上がらない= Defaulted/Partner 不一致の読解、
  MLAG スプリットブレイン、再起動復帰時の断= delay-restore)を記載。
- 決定事項: (1) 表記: 「リンクアグリゲーション」「LAG」「LACP」「MLAG」「ピアリンク」
  「スプリットブレイン」を標準表記とする。MLAG・ポートチャネル・ボンディング等は
  慣用名ルールを適用(仕様に不在の旨を本文で明記)。(2) 章の分担・範囲: ベンダー実装
  (vPC 等)の細部・DRNI・ICCP は存在の言及のみ。ESI-LAG の詳細は第2部05章の範囲の
  まま(本章は接続のみ)。Marker プロトコルは仕様の思想の説明にとどめた。
  (3) 本書の縦糸への接続: フェイルオープン(STP)vs フェイルクローズ(LACP)を
  第6部の総括軸に採用。MLAG の転送規則を DV・iBGP に続く「3度目のスプリット
  ホライズン」として明示。3×keepalive(BGP/RSTP/VRRP/LACP)、明示的合意
  (RSTP P/A・BGP 明示ピア)、ハッシュ分散= ECMP を参照。(4) 第6部の総括として
  「塞ぐ→速く直す→役を引き継ぐ→役を増やす→選出をなくす」の進化の一本道を
  まとめに記載。(5) 用語集へ5語登録(LACP、MLAG、スプリットブレイン、ピアリンク、
  リンクアグリゲーション)+ ECMP・ESI・STP の関連用語を更新。(6) **Step 27 からの
  宿題(roadmap への第6部組み込み)を解消**: 依存関係グラフに P6 を追加
  (P1→P6、P2→P6 のクロス依存)、各部の内容に第6部の表を追加、標準ルートを
  「…→ 第5部 → 第6部」に更新(第1部直後への前倒しも可と注記)、まとめを
  全6部構成に更新。(7) 「(後述: 04_lag_mlag.md)」参照をすべてリンク化
  (01章3箇所、02章1箇所、03章2箇所)。README の第6部を完成状態に更新
  (【執筆中】除去、04章リンク化、拡張予定表を「完成」に)。
- 未解決・要検証事項: (1) LACPDU の固定長 110 オクテット・Collector Information TLV の
  長さ(16)は 802.1AX の代表的記述に基づく(新しい版の Version 2 TLV 等の差分は
  扱っていない。条文の再確認が望ましい)。(2) WAITING の待ち合わせ「約2秒」
  (Aggregate Wait Time)は代表値として記載。(3) 「Collecting を先に有効化してから
  Distributing」は 802.1AX の independent control 前提の簡略化(coupled control では
  同時)。(4) Linux bonding の既定値(lacp_rate slow、xmit_hash_policy layer2、
  ad_select stable)と /proc/net/bonding の出力例はバージョンにより差がありうる。
  (5) MLAG の動作記述(キープアライブ、セカンダリ抑止、delay-restore)は各社実装の
  一般化であり、特定ベンダーの仕様・名称には立ち入っていない。(6) 「DRNI の実装は
  普及しなかった」は業界の一般的評価としての記載。(7) Marker プロトコルの使用実態
  (「単純に待つ」実装が多い)は慣行としての記述で、実装の網羅的確認はしていない。
- 次のステップ: なし(計画済みステップはすべて完了)。拡張する場合は第7部(QoS)等の
  章構成策定(CLAUDE.md へのステップ表追加)から着手する。

## Step 31: `07_qos/01_qos_basics.md` (完了日: 2026-07-10)

- 完了内容: 第7部(QoS)の起点となる QoS 基礎の章を執筆。「本書はここまで『届くか』
  だけを扱ってきた」という棚卸しから、ベストエフォート(RFC 791 の ToS =最初から
  あった品質の座席)、輻輳=統計多重化の代償(速度変換点/多対一の合流点という構造的
  輻輳点、超過が遅延と損失に両替される、マイクロバーストは5分平均に写らない)、
  「QoS は帯域を生まない — 輻輳の瞬間の先送・待機・廃棄を偶然から意図に変える
  ゼロサム」という正直な定義(輻輳がなければ不可視/全クラス最優先は無設定と同じ)、
  アプリごとの痛みの違いの表(VoIP 片道 150ms = ITU-T G.114、制御プレーンは
  3×キープアライブの裏返しで輻輳が障害に昇格)、遅延の4成分分解(直列化/伝搬/
  処理/キューイング — QoS が動かせるのはキューイング遅延だけ、10Mbps×1500B×40個
  = 48ms の算数)、ジッタ=キューイング遅延の変動(ジッタバッファは遅延への両替)、
  損失の二面性(TCP へのフィードバック信号 vs 実時間系の純劣化、ECN への伏線)、
  利用率と待ち時間の非線形性(M/M/1 の 1/(1−ρ) の形、バッファブロートへの伏線)、
  IntServ(RFC 1633/RSVP RFC 2205、ソフトステート、コアの状態爆発)vs DiffServ
  (RFC 2475、クラス集約+エッジ分類+コアは PHB、状態はパケットが荷札として運ぶ)
  を「状態をどこに置くか」(BGP フリーコア/FEC と同型)の再演として整理、
  マークの座席の表(DSCP/Traffic Class/PCP/MPLS TC — 処理する機器が見る層が違う
  から各層に座席)、DiffServ ドメインと信頼境界(DSCP はエンドツーエンドで尊重
  されない)、QoS パイプラインの ASCII 図(分類→マーキング→ポリシング→キュー
  イング→スケジューリング→シェーピング=第7部の地図)、FIFO+テールドロップ
  という最弱の既定、CS6 の慣行(第6部03の tcpdump `tos 0xc0` の伏線回収、
  RFC 4594、輻輳→セッション断→再収束→輻輳の増幅ループの Mermaid 図)、
  音声遅延予算 150ms のウォークスルー表、Linux 設定例(tc qdisc の fq_codel/
  pfifo_fast、`tc -s` の dropped/backlog の読み方、tcpdump での tos 確認、
  ping mdev)、トラブルシューティング4種(平均 30% で音声劣化=マイクロバースト、
  輻輳で BGP 断=CS6 とキューの両輪、DSCP が効かない3因、RTT 良好で
  スループット不足/速度良好で操作重い=損失 vs バッファブロートの切り分け)を記載。
- 決定事項: (1) **第7部の章構成を確定**: 01_qos_basics(輻輳・品質指標・
  IntServ/DiffServ・パイプライン)→ 02_classification_marking(DSCP/PHB、
  802.1p CoS、信頼境界)→ 03_queuing_scheduling(PQ/WFQ/DRR、WRED、AQM、
  バッファブロート)→ 04_shaping_policing(トークンバケット、三色マーカー、ECN)。
  CLAUDE.md に「拡張ステップ(第7部 QoS)」表(Step 31〜34)とディレクトリ構造・
  章立て大分類6を追記、README に第7部セクション(執筆中)を追加し拡張予定表を
  「執筆中」に更新(あわせて README 構成説明の「第6部(執筆中)」という
  古い記述を「完結」に修正)。(2) 表記: 「QoS」「輻輳」「ジッタ」「ベスト
  エフォート」「マーキング」「キューイング」「スケジューリング」「シェーピング/
  ポリシング」「DiffServ / IntServ」を標準表記とする。(3) 第7部は以後 DiffServ を
  前提とする旨を本文で宣言。DSCP の値の体系(EF/AF/CS)・PHB・層間写像は 02章、
  テールドロップの病理(グローバル同期)・AQM・バッファブロートは 03章、
  ECN・トークンバケットは 04章に委ねる(本文で明示)。(4) 本書の縦糸への接続:
  IntServ/DiffServ =「状態の置き場所」問題の再演(BGP フリーコア・FEC・
  フラッド&ラーン→EVPN と同系)、ソフトステート(RSVP)の伏線回収、
  3×キープアライブ=輻輳がプロトコル断に翻訳される裏返し、第6部03の
  `tos 0xc0` = CS6 の回収、冗長構成の片系障害時こそ輻輳する(第6部との接続)。
  (5) 機器内部の control-plane protection は言及のみ(本書の範囲外)。
  RSVP のプロトコル詳細も扱わない(第5部02の RSVP-TE の範囲のまま)。
  (6) 用語集へ6語登録(DiffServ、IntServ、QoS、ジッタ、輻輳、ベストエフォート)+
  ソフトステートの関連用語に IntServ を追加。
- 未解決・要検証事項: (1) M/M/1 の待ち時間の記述は「およそ 1/(1−ρ) に比例」と
  直感用に簡略化した(厳密には平均系内待ち W = 1/(μ−λ) 等。教科書的厳密性は
  意図的に落としている)。(2) Linux の既定 qdisc(fq_codel か pfifo_fast か)は
  カーネル/ディストリビューション依存と本文に明記したが、代表的な既定の変遷
  (systemd が net.core.default_qdisc を設定する件)には立ち入っていない。
  (3) 「OSPF/BGP/VRRP 実装は既定で CS6 を打つ」は広く見られる実装慣行としての
  記載(RFC 4594 はサービスクラスの指針であり、実装への要求ではない)。
  (4) 光ファイバの伝搬遅延 5µs/km、VoIP 片道 150ms(G.114)、符号化・
  パケット化 20〜30ms、ジッタバッファ 20〜60ms は代表値・目安としての記載。
  (5) incast という語は導入で言及したが用語集には登録していない(03章で
  キューイングの文脈で再登場したら要検討)。(6) 00_overview/01_roadmap.md への
  第7部組み込み(依存関係グラフ・読了ルート)は第7部完結時(Step 34)に行う
  (第6部のときと同じ運用)。
- 次のステップ: Step 32 `07_qos/02_classification_marking.md` から着手する

## Step 32: `07_qos/02_classification_marking.md` (完了日: 2026-07-10)

- 完了内容: 分類とマーキングの章を執筆。「分類=判断、マーキング=判断結果を荷札に
  キャッシュする」の分離から導入し(FEC と同型、ただしラベル=どこへ/DSCP=どう運ぶか
  の対比)、「DSCP は自己申告」という信頼問題を章の縦糸に設定。ToS オクテット3世代の
  再定義史(RFC 791 Precedence → RFC 1349 → RFC 2474 DSCP → RFC 3168 ECN、ビット図)、
  ToS バイト = DSCP × 4 の換算則(0xc0 = CS6、0xb8 = EF)、DSCP(名前空間)と
  PHB(動作)の分離設計とその理由、標準 PHB 4系統+LE(DF / CS = Precedence 後方互換 /
  EF = RFC 3246 ポリシングとセット / AF = RFC 2597 の 4×3 行列と 8x+2y、降格マーキング
  への伏線 / LE = RFC 8622)、RFC 4594 サービスクラス表(抜粋)、BA/MF 分類器
  (RFC 2475)と暗号化時代の「分類の重心の端点回帰」、PCP(802.1p は慣用名、
  1(BK) < 0(BE) の折れ曲がり、タグと運命を共にするリンクローカルな荷札、DEI)、
  層間写像(DSCP 上位3ビット ↔ PCP/TC、逆写像は不可逆)、トンネルモデル
  (RFC 2983 uniform/pipe、MPLS は RFC 3270、PHP で TC が消える罠の再訪)、
  信頼境界(信頼/再マーキング/洗い流しの3択、端末は信頼せず・IP 電話は条件付き・
  境界は契約、「端に置くが管理の外に出さない」)、ワイヤ上の3座席の図、DSCP 全値の
  換算表(2進/10進/ToS 16進)、音声パケットの荷札リレーのウォークスルー表(9区間、
  PCP/DSCP/TC の生滅)、Linux 設定例(nftables の dscp set ef と洗い流し、
  ping -Q 0xb8 での信頼境界探索、tcpdump -e での vlan p 5 と tos の同時観察)、
  トラブルシューティング4種(×4 換算ミスと ECN ビット誤爆、マーク消失の挟み撃ち
  特定と3犯人、PCP がタグなし区間/L3 ホップで消える、トンネル外側 DSCP = 0 と
  MPLS PHP)を記載。
- 決定事項: (1) 表記: 「PCP」を標準表記とし「CoS」「802.1p」は慣用名として併記
  (慣用名ルールの適用)。「クラスセレクタ(CS)」「信頼境界」を標準表記とする。
  DSCP の議論は IPv4/IPv6 共通である旨を本文に明記。(2) 章の分担: 廃棄優先度を
  読んで捨てる機構(WRED)は 03章、EF とセットのポリシング・三色マーカー・DEI の
  実用・ECN の動作は 04章に委ねる(本文で明示)。機器の trust dscp/cos の設定語彙、
  Linux の skb priority / egress-qos-map の配管詳細は本書の範囲外と明示。
  (3) 「意図をエンドツーエンドで運ぶ荷札は DSCP に一本化し、PCP/TC は区間ごとに
  再生成する使い捨ての要約」を第7部の設計原則として導入。(4) 前章 01 の
  「後述: 02_classification_marking.md」参照3箇所(写像・信頼境界・パイプライン)を
  リンク化。README の第7部の表で 02 章を執筆済みに更新。(5) 用語集へ7語登録
  (AF、DSCP、EF、PCP、PHB、クラスセレクタ、信頼境界)+ DiffServ・QoS の
  関連用語を更新。
- 未解決・要検証事項: (1) 802.1Q の traffic type 表(BK/BE/EE/CA/VI/VO/IC/NC)は
  版により表記・位置づけに揺れがある(1 < 0 の序列と用途名は広く一貫しているが、
  正確な Annex 番号・表番号は要確認のため本文では引用していない)。(2) RFC 4594 の
  サービスクラス表は抜粋・要約であり、Broadcast Video = CS3 等の細部は原文の
  ニュアンス(推奨であって要求ではない)を丸めている。(3) 「IP 電話を LLDP 等で
  識別して条件付き信頼する実装が多い」は実装慣行としての記載。(4) Linux vxlan/gre の
  外側 ToS 継承設定(tos inherit 等)は存在に言及したのみで、オプション名・既定値は
  実装/バージョン依存のため明記していない。(5) nftables の DSCP 名前定数
  (cs5/ef 等)と範囲構文はバージョンにより差がありうる。(6) 802.11e/WMM(無線の
  QoS 写像)は意図的に扱っていない(本書の範囲外のまま)。
- 次のステップ: Step 33 `07_qos/03_queuing_scheduling.md` から着手する

## Step 33: `07_qos/03_queuing_scheduling.md` (完了日: 2026-07-10)

- 完了内容: キューイングとスケジューリングの章を執筆。輻輳点の判断を「①廃棄の問い
  (損失の配分)/②順番の問い(遅延・帯域の配分)」の2軸に分解して章を構成。
  スケジューリング側: PQ(厳密優先、遅延・ジッタ上限が最大パケット1個分に縮む=
  01章の 48ms→1.2ms の回収、飢餓の毒、RFC 3246 のポリシング前提)、RR/WRR の
  バイト不公平 → GPS(流体の理想)→ WFQ(仮想終了時刻、O(log n)、論文由来と明記)
  → DRR(quantum/deficit、O(1)、帳簿のウォークスルー表)、実務の合成形
  (厳密優先1本+重み付き、CS6 は厳密優先でなく小保証キュー、LLQ は Cisco 慣用名)、
  work-conserving(重み=輻輳時の最低保証であり上限ではない)。廃棄側:
  テールドロップの3病理(満杯キュー恒常化/ロックアウト/グローバル同期、
  RFC 2309→RFC 7567 BCP 197)、AQM の発想(廃棄=送信者への意図的な輻輳信号、
  ECN への伏線)、RED(EWMA、min_th/max_th/max_p の折れ線図、チューニング問題)、
  WRED(AF 廃棄優先度の執行機構=02章の伏線回収、3面プロファイルの数値例、
  TCP 専用で実時間系に適用しない)、バッファブロート(RFC 970 の無限バッファ、
  BDP 目安)、CoDel(RFC 8289、滞在時間・target 5ms/interval 100ms・
  head drop・interval/√n、状態遷移図)、fq_codel(RFC 8290、DRR+フロー別 CoDel、
  新規フロー優遇=01章の既定 qdisc の伏線回収)。輻輳点の完全な合成図
  (クラスごとに廃棄戦略まで違う)、Linux 設定例(fq_codel 統計の精読、
  prio+u32 dsfield フィルタの最小 PQ、dsfield マスク 0xfc で ECN ビット回避、
  負荷中 ping での効果実測)、トラブルシューティング4種(PQ 飢餓=定員なし、
  グローバル同期=鋸歯で天井に届かない、バッファブロート=負荷中 ping での切り分け、
  WRED を実時間系に誤適用/AF 閾値逆転)を記載。
- 決定事項: (1) 表記: 「優先キューイング(PQ)」「テールドロップ」「バッファブロート」
  「AQM」を標準表記とする。WFQ・DRR・RED は RFC ではなく論文由来である旨を本文に
  明記(慣用名・出典ルールの適用)。LLQ・CoS 同様、LLQ は Cisco 慣用名と明記。
  (2) 章の分担: ポリシング(PQ の定員、三色マーカー)・シェーピング
  (non-work-conserving、ブロート輻輳点の引き取り)・ECN(廃棄の代わりの印)は
  すべて 04章に委ねる(本文で明示)。LFI は言及のみ。PIE(RFC 8033)は言及のみ。
  (3) 「厳密優先は1クラスだけ・必ずポリシングとセット」「WRED は TCP 系 AF にだけ、
  音声は PQ+ポリシング」「明示的クラス設計は WAN 境界等に、それ以外は fq_codel」を
  本書の実務の定石として明示。(4) ハードウェアキュー8本の伝統と PCP 3ビットの符合、
  「実務 4〜6 クラス」(02章)の背景として記載。(5) 01章の
  `03_queuing_scheduling.md` への後述参照5箇所、02章の1箇所をすべてリンク化。
  README の第7部の表で 03章を執筆済みに更新。(6) 用語集へ8語登録(AQM、
  CoDel / fq_codel、DRR、RED / WRED、WFQ、テールドロップ、バッファブロート、
  優先キューイング)+ AF(WRED)・QoS(AQM)・EF(優先キューイング)の関連用語を更新。
- 未解決・要検証事項: (1) 「出力ポートのハードウェアキューは8本が伝統的定番」は
  慣行としての記載(規格の要求ではない)。(2) WRED プロファイルの数値例
  (min_th 60/100/150 等)は説明用の架空値であり、推奨値ではない旨は文脈上明らかだが
  明記はしていない。(3) fq_codel の統計出力(new_flow_count 等の行)は iproute2 の
  バージョンにより表示に微差がありうる。(4) DRR の「1500B : 600B で帯域 23:1」の
  WRR 例は 1500/64 の比(約 23)を使った(600B の例とは別の数字。本文では
  64B と明記済みで矛盾なし)。(5) バッファサイジングの BDP 目安と 1/√n の解析
  (Appenzeller 2004)は出典名を挙げず「後年の解析」とのみ記載。(6) incast は
  03章では再登場しなかったため用語集登録は引き続き見送り(01章の言及のみ)。
  (7) L4S(RFC 9330 系)には触れていない。04章の ECN の文脈で言及するか要検討。
- 次のステップ: Step 34 `07_qos/04_shaping_policing.md` から着手する(第7部完結章。
  完結時に 00_overview/01_roadmap.md への第7部組み込み・README の【執筆中】表記の
  更新も行う)

## Step 34: `07_qos/04_shaping_policing.md` (完了日: 2026-07-12)

- 完了内容: シェーピングとポリシングの章を執筆(第7部完結、全ステップ完了)。
  03章のスケジューラ(比率)が答えられない「絶対量」の3つの問い(EF の定員/
  サブレート契約/輻輳点の引き取り)から導入し、「レートは時間窓なしに定義できない」
  という問題をトークンバケットの契約不等式(任意の窓 T で送出 ≤ CIR×T + CBS)で
  解決する構成。トークンバケット(CIR/CBS、DRR の deficit と同型の「バイトの帳簿」、
  リーキーバケット言及)、ポリシング(遅延ゼロの入口検認、RFC 3246 EF 定員の回収、
  TCP バーストとの不和= CBS ≥ CIR×RTT の慣行)、シェーピング(本書唯一の
  non-work-conserving、超過の遅延両替=バックログ÷CIR、シェーパ配下に fq_codel を
  張る輻輳点の引き取りの完成)、対比表と定石(受け取る側はポリス・送る側はシェープ)、
  三色マーカー(srTCM=RFC 2697 のカスケード2バケット/trTCM=RFC 2698 の2レート、
  yellow の意味の違い、color-blind/aware、AF 降格→WRED 執行の分業完成、DEI・MEF
  帯域プロファイルとの同型)、ECN(RFC 3168、ECT/CE の2段設計の理由、ECE/CWR の
  交渉とエコーの Mermaid 図、CE 1個=損失1個の等価則、AQM なしの ECN は存在しない、
  RFC 6040、bleaching と 0xfc マスクの完結、L4S=RFC 9330〜9332 言及)、
  ウォークスルー2本(同じ到着列でのポリサ/シェーパの運命対比表、srTCM/trTCM の
  アルゴリズム)、パイプライン完成図(01章の地図に全部品・章番号を記入)、
  Linux 設定例(tbf の rate/burst/limit = CIR/CBS/最大遅延、tbf+fq_codel、
  prio への police 重ね= 03章の宿題回収、tcp_ecn と tcpdump での [SEW]/ECT/CE)、
  トラブルシューティング4種(ポリサ×TCP バーストの単一ストリーム低速、
  シェーパ limit 過大の自作ブロート、EF 定員の見積もり乖離、ECN 不発の3因)を記載。
- 決定事項: (1) 表記: 「トークンバケット」「三色マーカー」「ECN」を標準表記とし、
  リーキーバケットはトークンバケットに統一する旨を本文に明記。CBS ≥ CIR×RTT・
  シェーピングは実効速度の 90〜95%・EF 定員は設計同時数+余裕、は RFC 外の
  慣行・目安と明記。(2) 第7部完結に伴う横断更新: 00_overview/01_roadmap.md へ
  第7部を組み込み(7部構成へ、依存グラフに P1→P7、各部の内容に第7部の表、
  標準ルート・まとめを更新)。README の第7部【執筆中】/04章【執筆予定】を解消し
  拡張予定表を「完成」に更新。(3) L4S(RFC 9330〜9332、DCTCP=RFC 8257)は
  Step 33 の宿題どおり ECN の節で存在の言及のみ(本書の範囲外)。cake qdisc も
  言及のみ。アドミッション制御は症状3で一言のみ。(4) 01章2箇所・02章3箇所・
  03章7箇所の「後述: 04_shaping_policing.md」参照をすべてリンク化(第7部内の
  未解消参照はゼロに)。(5) 用語集へ4エントリ登録(ECN、三色マーカー、
  シェーピング / ポリシング、トークンバケット)+ AF・AQM・RED/WRED・QoS・EF の
  関連用語を更新。
- 未解決・要検証事項: (1) trTCM の green が「Tp と Tc の両方から引く」という記述は
  RFC 2698 のアルゴリズムに基づくが、条文の変数名(Tp/Tc)対応は要再確認。
  (2) tc police の `conform-exceed drop` 構文・tbf の burst 下限(タイマ粒度由来)は
  カーネル/iproute2 のバージョン依存がありうる(動作原理の説明には影響なし)。
  (3) `net.ipv4.tcp_ecn` の既定値 2(受動のみ)は代表的な既定としての記載
  (ディストリビューション依存)。(4) 「シェーピングは実効速度の 90〜95%」は
  経験則であり、回線種別(可変レートの無線・DOCSIS 等)では成り立たないことがある。
  (5) SYN-ACK に ECE+CWR 両方が立つケースを「不正」とした説明は RFC 3168 の
  ネゴシエーション規則の要旨(正確な条文表現は要再確認)。(6) incast の用語集登録は
  第7部完結まで再登場がなかったため最終的に見送り(01章の言及のみ)。
- 次のステップ: 計画済みステップはすべて完了。次の拡張分野は「現在の状態」の
  記載どおり、CLAUDE.md への拡張ステップ表追記から始める
